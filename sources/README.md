# Local Source Papers

Place local PDFs, extracted paper text, and large source material here.

This directory is intentionally ignored by Git except for this README. Track bibliographic metadata in `references.bib` and structured notes in `notes/`.
