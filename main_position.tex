\documentclass{clv2025}

\jvol{vv}
\jnum{nn}
\jyear{2026}
\jinfo{}
\renewcommand{\footmark}{}
\dochead{Position Paper}

\usepackage[T1]{fontenc}
\usepackage[american]{babel}
\usepackage{natbib}
\bibliographystyle{compling}
\usepackage{mathtools}
\usepackage{amssymb}
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{array}
\usepackage{graphicx}
\usepackage{xcolor}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,calc}
\usepackage{placeins}
\hypersetup{
  hidelinks,
  pdftitle={Hallucination Is Relative: Evaluating LLM Divergence under Truth Contracts},
  pdfauthor={Mathis Carlesso, Martino Lovisetto, Elod Egyed-Zsigmond, Pierre-Yves Genest, Stefan Duffner},
  pdfsubject={Position paper in computational linguistics},
  pdfkeywords={large language models, hallucination evaluation, truth contract,
    factuality, faithfulness, licensed divergence, creativity evaluation}
}

\newcommand{\TC}{\textsc{TC}}
\newcommand{\LD}{\textsc{LD}}
\newcommand{\Hall}{\textsc{H}}
\newcommand{\Grd}{\textsc{G}}
\newcolumntype{Y}{>{\raggedright\arraybackslash}X}
\newcolumntype{P}[1]{>{\raggedright\arraybackslash}p{#1}}

% Canonical terminology:
% task oracle O, permission scope Gamma, required status marking mu
% requested style-marking level sigma, OUTSIDE the truth contract
% evidence states: ENTAILED / CONTRADICTED / UNKNOWN
% claim labels: G (grounded) / H (hallucination) / LD (licensed divergence)
% "response-level record" = the whole three-component output; "content verdict"
% = its content component only (contract-compliant / contract-violating /
% indeterminate). Never write "structured verdict" or "response-level verdict".

\definecolor{clrH}{RGB}{155,18,18}
\definecolor{clrLD}{RGB}{22,98,48}
\definecolor{clrB}{RGB}{22,52,125}
\definecolor{clrP}{RGB}{92,61,145}
\definecolor{clrO}{RGB}{170,91,24}
\definecolor{clrG}{RGB}{65,65,65}
\definecolor{bgB}{RGB}{245,247,254}
\tikzset{
  input/.style={rectangle,rounded corners=4pt,draw=gray!55,fill=gray!8,
    font=\small\itshape,text width=5.0cm,align=center,inner sep=6pt},
  decision/.style={rectangle,rounded corners=3pt,draw=clrB!55,fill=bgB,
    font=\small,text width=5.2cm,align=center,inner sep=6pt},
  verdict/.style={rectangle,rounded corners=3pt,draw=clrG,fill=gray!8,
    font=\small\bfseries\sffamily,inner xsep=7pt,inner ysep=4pt},
  verdictH/.style={verdict,draw=clrH,text=clrH,fill=clrH!7},
  verdictLD/.style={verdict,draw=clrLD,text=clrLD,fill=clrLD!7},
  arrow/.style={-{Stealth[length=5pt,width=3.5pt]},thick,color=gray!70!black},
  edge/.style={font=\scriptsize\sffamily,fill=white,inner sep=1.5pt}
}

\title{Hallucination Is Relative:
Evaluating LLM Divergence under Truth Contracts}
\runningtitle{Contract-Relative Hallucination Evaluation}
\runningauthor{Carlesso et al.}

\author{Mathis Carlesso$^{1,2}$, Martino Lovisetto$^{2}$, El\H{o}d Egyed-Zsigmond$^{1}$, Pierre-Yves Genest$^{2}$, Stefan Duffner$^{1}$}

\affilblock{
  \affil{INSA Lyon, CNRS, LIRIS, UMR5205, Universit\'e Claude Bernard Lyon 1, 69621 Villeurbanne, France\\
  \quad \email{mathis.carlesso@insa-lyon.fr}}
  \affil{Alteca, 69100 Villeurbanne, France}
}

\begin{document}
\maketitle

\begin{abstract}
The same sentence can be a hallucination in one task and an acceptable
hypothesis in another. Evidence alone cannot explain this difference: the
source and the claim may be unchanged while the instructions differ. Even as
models become larger and more capable, hallucinations persist; this
suggests that evaluations should distinguish harmful errors from clearly marked
departures that may be useful in creative tasks. We propose the
\emph{truth contract}, which states what evidence counts, what the
task allows beyond it, and how uncertainty must be marked. At claim level, it
separates grounded claims and hallucinations from \emph{licensed
divergence} (content that goes beyond the evidence but is allowed and clearly
marked). In a purposive mapping of current evaluation resources, the selected
designs leave two aspects disconnected: factuality and faithfulness datasets
check claims against evidence without asking whether a task permits useful
divergence, whereas literary-creativity, problem-solving, and code-generation
datasets focus on novelty, quality, feasibility, or task success without
explicitly representing which claims must remain factually true. None explicitly
combines permission beyond the evidence with a requirement to mark such
divergence. This finding concerns only the selected resources. The paper
does not evaluate models; empirical validation remains future work.
\end{abstract}

\section{Introduction}\label{sec:intro}

Large language models (LLMs) are used for tasks that range from knowledge-intensive
question answering and document summarization to explanation, brainstorming, and
creative writing. Across these uses, fluency is not enough: a response must make
appropriate factual commitments, communicate uncertainty when needed, and serve
the purpose of the task. Hallucination evaluation is therefore a reliability
problem, but also a question of what should count as an error in context.

LLMs can produce fluent claims that are contradicted by the available evidence
or cannot be verified from it
\citep{ji_survey_2023,huang_survey_2025}.
Such outputs can mislead users and can limit the use of LLMs in high-stakes
applications. Research has therefore defined types of hallucination, built
benchmarks to measure them, developed detection methods, and proposed
mitigation strategies
\citep{ji_survey_2023,huang_survey_2025}.
Even as newer models improve on general capabilities, AA-Omniscience shows that
they still hallucinate and that larger open-weight models do not consistently
hallucinate less \citep{jackson_aaomniscience_2025}.

That conclusion matters because LLM deployments do not all ask for the same kind
of response. In factual question answering or source-grounded summarization,
unsupported content is normally an error. In an explanation, however, an
analogy may be useful without being literally true; brainstorming and fiction
may invite invention; and hypothesis generation may ask for possibilities that
the available evidence leaves unsettled
\citep{franceschelli_creativity_2024,jiang_survey_2024,rick_supermind_2023}.
Creativity research commonly distinguishes novelty from usefulness or quality
\citep{runco_standard_2012,diedrich_are_2015}, while style-transfer research
treats the form of a response as a separate, controllable property
\citep{kang_style_2021}. A metaphor may therefore be valuable precisely because
it is not a literal restatement of the source, while a factually correct answer
may still fail a requested style. Factuality, source faithfulness, creativity,
and response style consequently answer different evaluation questions.

An evidence-unknown claim is therefore not automatically an error. A clinical
assistant asked what a patient's symptoms might indicate may raise early Lyme
disease as a possibility, but must not present it as established. The crucial
distinction is between introducing a possibility within the task's scope and
asserting it as a fact.

These settings require two judgments. First, does the available information
settle the claim? Second, if it does not, did the task authorize the response to
introduce that claim? We argue that hallucination judgments need both, whereas
many evidence-based protocols operationalize only the first.
Figure~\ref{fig:prompt-to-claim} illustrates the contrast with the museum-label
task used throughout the paper.



The museum example does not make factuality or source-faithfulness evaluation
unnecessary; it clarifies their scope. Some tasks ask a model to reproduce facts
from external knowledge, whereas others ask it to stay faithful to a supplied
document. Still others ask it to solve a problem, explain an idea with an
analogy, or write in a particular style. A statement that is not literally
supported by the source may be useful in the latter tasks but erroneous in the
former. We therefore treat factuality, source faithfulness, creativity, and
response style as related but distinct requirements.

Factuality evaluation compares generated claims with external knowledge
sources, whereas source-faithfulness evaluation compares them with a provided
source document
\citep{maynez_faithfulness_2020,kryscinski_evaluating_2020,min_factscore_2023}.
In the framework developed below, these resources instantiate the task oracle.

Creativity research commonly distinguishes novelty from usefulness or quality
\citep{runco_standard_2012,diedrich_are_2015};
CreativityPrism, for example, evaluates quality, novelty, and diversity
\citep{hou_creativityprism_2025}, and recent surveys and task-specific novelty
metrics continue to refine these criteria
\citep{ismayilzada_progress_2024,zhao_assessing_2025,namuduri_genie_2026}.
These dimensions do not by themselves determine whether a novel factual claim
violates a task constraint.
Two protocols can therefore assign different labels to identical wording when
their prompts establish different task conditions.

\paragraph{Our position}
We propose a claim-level evaluation rule under which the hallucination label is
relative to the task's truth contract. By \emph{relative}, we mean that the
evaluation label depends on the contract; the truth value of the underlying
proposition does not.

A \emph{claim} is a truth-conditional commitment attributed to the response:
something the response presents as being the case and that can therefore be
checked against evidence. An \emph{evaluator}, which may be a human annotator,
an automated pipeline, or both, identifies these commitments.

We write $p$ for the complete task context. It includes the prompt, applicable
system instructions, dialogue history, domain constraints, attached data, and
any designated tool or environment state.

Evaluating a claim takes two distinct steps. First, claim verification assigns
one of three \emph{evidence states} relative to the standard the task uses to
settle claims, which we call its \emph{task oracle}: \textsc{entailed},
\textsc{contradicted}, or \textsc{unknown}. Here, \textsc{unknown} means only
that the task oracle neither entails nor contradicts the claim; it is not a
statement about all possible knowledge.

Second, the framework maps that evidence state to one of three \emph{claim
labels}: grounded, hallucination, or licensed divergence. Entailed claims are
grounded and contradicted claims are hallucinations; only \textsc{unknown}
claims need the permission-scope and required-status-marking checks.

Figure~\ref{fig:prompt-to-claim} makes this distinction concrete. The claim
that the complete Rosetta Stone stood near a temple entrance is neither
entailed nor contradicted by the frozen excerpt, so its evidence state is
\textsc{unknown} in both tasks. The factual prompt $p_1$ forbids an added
reconstruction and therefore labels the claim hallucination; the creative
prompt $p_2$ permits one clearly marked reconstruction and therefore labels the
same claim licensed divergence. Figure~\ref{fig:profiles} shows the broader
design implication: the selected strict-grounding resources and creative suites
are different nominal task profiles, while the highlighted gap is the absence
of a selected resource that combines explicit contract fields with
claim-preserving style variation. The evidence state stays the same; only the
task-level label changes.

We write the contract as $\TC=(O,\Gamma,\mu)$. Its three fields have separate
roles. The task oracle $O$ settles the evidence state. The permission scope
$\Gamma$ specifies which evidence-unknown content the task authorizes. The
required status marking $\mu$ specifies how that content must be presented.
\FloatBarrier

\FloatBarrier
\begin{figure}[!t]
\centering
\resizebox{0.90\linewidth}{!}{%
\begin{tikzpicture}[x=1cm,y=1cm]
  \tikzset{
    taskbox/.style={
      decision,text width=5.00cm,minimum height=1.45cm,align=left,
      font=\scriptsize,inner sep=4pt},
    contractbox/.style={
      decision,text width=5.00cm,minimum height=1.45cm,align=left,
      font=\scriptsize,inner sep=4pt},
    contextbox/.style={
      rectangle,rounded corners=4pt,draw=clrO!60,fill=clrO!6,
      text width=10.35cm,align=left,font=\scriptsize,inner sep=4pt},
    sharedspan/.style={
      input,text width=9.80cm,minimum height=1.15cm,align=left,
      font=\scriptsize,inner sep=4pt},
    resultH/.style={
      rectangle,rounded corners=4pt,draw=clrH,fill=clrH!7,
      text=clrH,text width=5.00cm,minimum height=0.95cm,
      align=center,font=\scriptsize,inner sep=4pt},
    resultLD/.style={
      rectangle,rounded corners=4pt,draw=clrLD,fill=clrLD!7,
      text=clrLD,text width=5.00cm,minimum height=0.95cm,
      align=center,font=\scriptsize,inner sep=4pt},
    figure-note/.style={
      rectangle,rounded corners=3pt,draw=clrG!45,fill=gray!4,
      font=\scriptsize\sffamily\bfseries,align=center,
      text width=7.90cm,inner sep=4pt},
    figure-arrow/.style={arrow,shorten <=2pt,shorten >=2pt},
    figure-top-arrow/.style={arrow,shorten <=0pt,shorten >=2pt}
  }

  \node[contextbox] (context) at (0,0)
    {\textbf{Task oracle $O$, shared by both tasks:}
     \href{https://en.wikipedia.org/wiki/Rosetta_Stone}{Wikipedia article
     ``Rosetta Stone''}, frozen to this excerpt: ``A stele found near Rosetta
     in 1799 bears a decree from 196 BCE in hieroglyphic, Demotic, and Greek.
     It is a fragment of a larger stele probably once displayed in a
     temple.''};

  \node[taskbox,anchor=north east] (strict)
    at ([xshift=-0.28cm,yshift=-0.80cm]context.south)
    {\textbf{Factual museum-label task ($p_1$)}\\[1pt]
     ``Write a short factual museum label for a general audience.
     Use only facts from the source.''};
  \node[taskbox,anchor=north west] (creative)
    at ([xshift=0.28cm,yshift=-0.80cm]context.south)
    {\textbf{Creative museum-label task ($p_2$)}\\[1pt]
     ``Write a short museum label for children, in a curious and imaginative
     voice. Mark uncertainty clearly. You may add one plausible
     reconstruction.''};

  \coordinate (sourcebus) at ([yshift=-0.22cm]context.south);
  \coordinate (strictbranch) at (strict.north |- sourcebus);
  \coordinate (creativebranch) at (creative.north |- sourcebus);
  \draw[semithick,draw=black!55] (context.south) -- (sourcebus);
  \draw[semithick,draw=black!55]
    (strictbranch) -- (creativebranch);
  \draw[figure-top-arrow] (strictbranch) -- (strict.north);
  \draw[figure-top-arrow] (creativebranch) -- (creative.north);

  \node[contractbox,anchor=north] (tc1)
    at ([yshift=-0.70cm]strict.south)
    {\textbf{Truth contract 1:} $\TC(p_1)=(O,\Gamma_1,\mu)$\\[1pt]
     \emph{Permission scope} $\Gamma_1$: no added reconstruction\\
     \emph{Required status marking} $\mu$: uncertainty stated clearly};
  \node[contractbox,anchor=north] (tc2)
    at ([yshift=-0.70cm]creative.south)
    {\textbf{Truth contract 2:} $\TC(p_2)=(O,\Gamma_2,\mu)$\\[1pt]
     \emph{Permission scope} $\Gamma_2$: one plausible reconstruction\\
     \emph{Required status marking} $\mu$: uncertainty stated clearly};

  \coordinate (contract-row-bottom) at ($(tc1.south)!0.5!(tc2.south)$);
  \node[sharedspan,anchor=north] (span)
    at ([yshift=-0.40cm]contract-row-bottom)
    {\textbf{\upshape Identical response in both tasks:}
     ``Think of the Rosetta Stone as the same message written three ways:
     in hieroglyphic, Demotic, and Greek.
     \textbf{One possible reconstruction places the complete stele near a
     temple entrance, like a public noticeboard carved in stone.}''\\[2pt]
     \textbf{\upshape Claim checked:} the complete stele stood near a temple
     entrance. The excerpt neither confirms nor rejects it.};

  \coordinate (result-row-top) at ([yshift=-0.60cm]span.south);
  \node[resultH,anchor=north east] (hall)
    at ([xshift=-0.20cm]result-row-top)
    {\textbf{\mbox{HALLUCINATION (\Hall)}}\\[1pt]
     The source is silent and this task allows no invention.};
  \node[resultLD,anchor=north west] (licensed)
    at ([xshift=0.20cm]result-row-top)
    {\textbf{\mbox{LICENSED DIVERGENCE} (\LD)}\\[1pt]
     The source is silent, but one marked invention is allowed.};

  \draw[figure-arrow] (strict.south) -- (tc1.north);
  \draw[figure-arrow] (creative.south) -- (tc2.north);
  \draw[figure-arrow] (tc1.south) -- (span.north -| tc1.south);
  \draw[figure-arrow] (tc2.south) -- (span.north -| tc2.south);
  \draw[figure-arrow] (span.south -| hall.north) -- (hall.north);
  \draw[figure-arrow] (span.south -| licensed.north) -- (licensed.north);

  \node[figure-note,anchor=north]
    at ([yshift=-0.36cm]$(hall.south)!0.5!(licensed.south)$)
    {Only the permission scope $\Gamma$ changes inside the truth contract;\\
     requested writing style is evaluated separately.};
\end{tikzpicture}%
}
\caption{\textbf{Same source and claim, different permissions.}
Both truth contracts use the same task oracle (the frozen source), response,
and required status marking. Only the permission scope $\Gamma$ differs. The
oracle leaves the claim \textsc{unknown} in both tasks. The factual task permits
no reconstruction and therefore assigns the hallucination label (\Hall). The
creative task permits one clearly marked reconstruction and therefore assigns
the licensed-divergence label (\LD). The prompts also request different writing
styles, but requested style is evaluated separately and does not affect either
label. Licensed divergence records permission and adequate marking, not truth.}
\label{fig:prompt-to-claim}
\end{figure}

The evaluator also checks how the response presents each claim. In the Rosetta
Stone example, the phrase ``One possible reconstructionldots'' presents the
claim as a possibility, rather than as an established fact. This is the
response's \emph{observed status marking}: a claim can be presented as
established, hedged, or explicitly hypothetical. The evaluator compares this
observed marking with the marking required by the task, $\mu$. If the response
breaks either the task's permission or its marking requirement, a
\emph{reason code} records why.

The labels can then be read directly from the example. A claim that the source
entails receives the grounded label (\Grd); a claim that contradicts the source
receives the hallucination label (\Hall), with reason code
\textsc{contradicted}. If the source is silent, the evaluator asks whether the
claim was allowed. In the factual museum-label task, the reconstruction is not
allowed, so the claim receives \Hall{} with reason code
\textsc{out-of-scope}. In the creative task, one reconstruction is allowed,
and ``One possible reconstructionldots'' marks it clearly, so the claim
receives licensed divergence (\LD). If the response instead stated ``The
complete stele stood near a temple entrance'' as a fact, it would fail the
marking requirement and receive \Hall{} with reason code
\textsc{marking-failure}. Figure~\ref{fig:prompt-to-claim} illustrates this
\textsc{unknown} branch. Section~\ref{sec:framework} defines the normalized
claim representation and the complete rule.

The framework evaluates individual truth-conditional claims.
It does not by itself assess global logical validity, cross-claim coherence,
response completeness, or answer relevance.
At response level, the framework reports a three-component record: a content
verdict, style-marking alignment, and creative-task success. The content verdict
is derived from the claim trace together with claim-recovery coverage and any
blocking truth-contract, task-oracle, or adjudication failure. The other two
components never change a claim label.

\paragraph{Contributions}
This position paper makes three contributions.
First, it introduces the truth contract $\TC=(O,\Gamma,\mu)$ and a
claim-labeling rule that separates the task oracle, permission scope, and
required status marking.
Second, it derives a three-component response-level record consisting of a
content verdict, style-marking alignment, and creative-task success.
Third, it uses a purposive resource mapping and worked cases to identify missing
evaluation combinations and to derive a falsifiable research agenda.

\paragraph{Truth contracts versus separate factuality and instruction-following judgments}
Evidence-based factuality and source-faithfulness methods ask whether a claim
is supported by a designated reference, whereas instruction-following and
intent-aware evaluation ask whether the response satisfies or correctly
interprets task constraints
\citep{maynez_faithfulness_2020,min_factscore_2023,zhou_ifeval_2023,hao_faithqa_2025}.
These judgments are complementary and may need to be reconciled explicitly
\citep{wu_dancing_2024}. Neither output alone decides whether an
\textsc{unknown} claim receives the hallucination or licensed-divergence label.
To make that decision, a rubric must represent $O$, $\Gamma$, and $\mu$ and
apply the same claim-level rule. Such a rubric instantiates the truth-contract
rule.

\paragraph{A secondary problem: claim recovery under stylistic variation}
Applying the rule first requires the evaluator to recover the intended claims.
Highly stylized wording may make recovery harder. Surface style, considered
apart from status marking, remains outside the truth contract.
Section~\ref{subsec:style} defines the variable, and
Appendix~\ref{app:diagnostics} gives the stability test.


\paragraph{Mapping and worked cases}
The purposive mapping codes forty-one resources for the three contract fields
and for whether they vary requested style marking while holding canonical claim
content fixed. It identifies patterns and missing combinations within the
selected sample, not their prevalence in the wider literature. Five worked
cases state what is fixed, what changes, and whether the rule predicts a change
or stability in claim recovery and labeling. These cases are specification
checks rather than an annotation study. Section~\ref{sec:agenda} turns that
limitation into empirical questions.

\paragraph{Organization}
Sections~\ref{sec:framework} and~\ref{sec:related} define the framework and
position it against neighboring evaluation paradigms.
Sections~\ref{sec:methodology}--\ref{sec:conclusion} present the mapping,
worked cases, research agenda, discussion, and conclusion. The appendices
specify the response-level diagnostics, publish the resource coding, and report
the leaderboard measurements cited in Section~\ref{sec:discussion}.

\section{Framework}\label{sec:framework}

The framework assigns separate roles to evidence, authorization, and
presentation. Different evaluators should reach the same judgment when applying the framework to the same response. This section therefore defines the roles of evidence, authorization, and presentation precisely.


\subsection{Framework Overview}

Consider the running example used throughout this paper: the Rosetta Stone
museum label of Figure~\ref{fig:prompt-to-claim}.
The task context supplies the frozen Wikipedia excerpt as the task oracle, asks
for a museum label whose historical statements stay faithful to it, and permits
one plausible reconstruction provided it is marked as conjecture.
Suppose the response contains the span ``One possible reconstruction places the
complete stele near a temple entrance.''
The evaluator first interprets the span in this task context. It recovers one
contextualized claim: the complete stele stood near a temple entrance.

The evaluator then canonicalizes the claim into two components. The normalized
content $q^*$ records what is claimed, including entities, predicates,
negation, quantities, and temporal scope. The observed status marking $m^*$
records how the response presents the claim, including epistemic modality and
relevant discourse framing. Here, $m^*$ records that the placement is offered
as possible rather than asserted as established.
\begin{figure}[!t]
\centering
\resizebox{\textwidth}{!}{%
\begin{tikzpicture}[x=1cm,y=1cm,font=\small,
  every node/.style={inner sep=0pt}]
  \definecolor{figblue}{RGB}{31,67,140}
  \definecolor{figorange}{RGB}{176,103,42}
  \definecolor{figgreen}{RGB}{42,118,67}
  \definecolor{figred}{RGB}{162,54,42}
  \tikzset{
    fbox/.style={rectangle,rounded corners=3pt,draw=#1!55,fill=#1!5,
      align=center,inner sep=5pt},
    fbox/.default=black,
    prompt/.style={fbox=figblue,text width=6.30cm,minimum height=2.25cm,
      font=\footnotesize},
    archive/.style={fbox=figorange,text width=6.00cm,minimum height=2.25cm,
      align=left,font=\footnotesize},
    contract/.style={fbox=figblue,text width=3.15cm,minimum height=1.45cm,
      font=\footnotesize},
    stylebox/.style={fbox=figblue,dashed,text width=3.15cm,minimum height=1.45cm,
      font=\footnotesize},
    response/.style={fbox=black,text width=11.0cm,minimum height=1.70cm,
      font=\footnotesize,align=left,inner sep=7pt},
    span/.style={fbox=black,text width=2.55cm,minimum height=1.16cm,
      font=\scriptsize,align=left,inner sep=5pt},
    claim/.style={fbox=black,text width=2.55cm,minimum height=1.60cm,
      font=\scriptsize,align=left,inner sep=5pt},
    side/.style={fbox=black,text width=3.05cm,font=\scriptsize,
      align=left,inner sep=5pt},
    state/.style={font=\scriptsize\sffamily\bfseries,text=black!60,
      align=center},
    verdict/.style={rectangle,rounded corners=3pt,draw=#1!75,fill=#1!5,
      minimum width=0.62cm,minimum height=0.48cm,align=center,
      font=\small\sffamily\bfseries,text=#1,inner sep=3pt},
    verdict/.default=black,
    flow/.style={-{Stealth[length=4pt,width=3pt]},semithick,draw=black!55},
    guide/.style={draw=#1!65,line width=0.9pt},
    guide/.default=figblue,
    lab/.style={font=\scriptsize\sffamily\bfseries,text=black!65,
      align=right,anchor=east},
    note/.style={font=\scriptsize,align=center,anchor=north,text width=2.75cm}
  }

  % Shared grid: all four claim pipelines use the same x coordinates.
  \def\xone{-5.10}
  \def\xtwo{-2.15}
  \def\xthree{0.80}
  \def\xfour{3.75}
  \def\xright{3.80}
  \def\xlabel{-7.15}

  % Task context
  \node[lab] at (\xlabel,7.55) {task context};
  \node[prompt,text width=10.60cm] (prompt) at (-0.50,7.55)
    {\textbf{Prompt:} ``Using the
    frozen Wikipedia excerpt on the Rosetta Stone, write a museum label for
    young visitors that brings the stone and its significance to life.
    Keep historical statements faithful to the excerpt. Where it is silent,
    you may add one plausible reconstruction, marked as conjecture.''};

  % All three truth-contract fields share one visual row.
  \node[lab] at (-7.75,4.85)
    {truth contract};
  \node[archive,anchor=east] (archive) at (-1.50,4.85)
    {\textbf{Task oracle $O$}\\
    \href{https://en.wikipedia.org/wiki/Rosetta_Stone}{Wikipedia article
    ``Rosetta Stone''}\\[-1pt]
    ``Found near Rosetta in 1799; 196 BCE decree in hieroglyphic, Demotic,
    and Greek; fragment of a larger stele probably once displayed in a
    temple.''};
  \node[contract,text width=2.90cm,minimum height=2.25cm]
    (scope) at (0.35,4.85)
    {$\Gamma$ \emph{permission scope}\\Rosetta Stone facts and one\\marked historical reconstruction};
  \node[contract,text width=2.90cm,minimum height=2.25cm]
    (mark) at (3.75,4.85)
    {$\mu$ \emph{required status marking}\\conjecture stated as such};

  % Prompt-to-contract bus and arrows into the response context.
  \coordinate (busleft) at ($(archive.north)+(0,0.28)$);
  \coordinate (busright) at ($(mark.north)+(0,0.28)$);
  \coordinate (busprompt) at (prompt.south |- busleft);
  \draw[semithick,draw=black!55] (prompt.south) -- (busprompt);
  \draw[semithick,draw=black!55] (busleft) -- (busright);
  \foreach \field in {archive,scope,mark}
    \draw[flow] (\field.north |- busleft) -- (\field.north);

  % Response
  \node[lab] at (\xlabel,2.25) {response};
  \node[response] (response) at (-0.50,2.25) {\textbf{Model response:}
    ``Rosetta, 1799.\ The stone was found near Rosetta. Its decree appears
    in hieroglyphic, Demotic, and Greek. One possible reconstruction places
    the complete stele near a temple entrance, like a public noticeboard
    carved in stone.\ The decree dates from 1799.''};
  \foreach \field in {archive,scope,mark}
    \draw[flow] (\field.south) -- (\field.south |- response.north);

  % Span typing
  \node[lab] at (\xlabel,0.25) {response spans};
  \node[span] (s1) at (\xone,0.25)
    {$s_1$ ``found near Rosetta\\in 1799''\\[-1pt]\emph{claim-only}};
  \node[span] (s2) at (\xtwo,0.25)
    {$s_2$ ``decree appears in\\three scripts''\\[-1pt]\emph{claim-only}};
  \node[span] (s3) at (\xthree,0.25)
    {$s_3$ ``complete stele near\\a temple entrance\ldots''\\[-1pt]\emph{claim + status marking}};
  \node[span] (s4) at (\xfour,0.25)
    {$s_4$ ``decree dates\\from 1799''\\[-1pt]\emph{claim-only}};
  \foreach \sp in {s1,s2,s3,s4}
    \draw[flow] (response.south -| \sp.north) -- (\sp.north);

  % Canonical claims
  \node[lab] at (\xlabel,-1.55)
    {canonical claims};
  \node[claim] (c1) at (\xone,-1.55)
    {$q^*_1$: found near Rosetta\\in 1799\\$m^*_1$: asserted};
  \node[claim] (c2) at (\xtwo,-1.55)
    {$q^*_2$: decree in\\hieroglyphic, Demotic,\\and Greek\\$m^*_2$: asserted};
  \node[claim] (c3) at (\xthree,-1.55)
    {$q^*_3$: complete stele\\stood near a temple\\entrance\\$m^*_3$: possible};
  \node[claim] (c4) at (\xfour,-1.55)
    {$q^*_4$: decree dates\\from 1799\\$m^*_4$: asserted};
  \foreach \sp/\cl in {s1/c1,s2/c2,s3/c3,s4/c4}
    \draw[flow] (\sp.south) -- (\cl.north);

  % Evidence state
  \node[lab] at (\xlabel,-3.28)
    {evidence state};
  \node[state] (e1) at (\xone,-3.28) {ENTAILED};
  \node[state] (e2) at (\xtwo,-3.28) {ENTAILED};
  \node[state] (e3) at (\xthree,-3.28) {UNKNOWN};
  \node[state] (e4) at (\xfour,-3.28) {CONTRADICTED};
  \foreach \cl/\ev in {c1/e1,c2/e2,c3/e3,c4/e4}
    \draw[flow] (\cl.south) -- (\ev.north);

  % Claim labels
  \node[lab] at (\xlabel,-4.63)
    {claim label};
  \node[verdict=black] (v1) at (\xone,-4.63) {\Grd};
  \node[verdict=black] (v2) at (\xtwo,-4.63) {\Grd};
  \node[verdict=figgreen] (v3) at (\xthree,-4.63) {\LD};
  \node[verdict=figred] (v4) at (\xfour,-4.63) {\Hall};
  \foreach \ev/\ve in {e1/v1,e2/v2,e3/v3,e4/v4}
    \draw[flow] (\ev.south) -- (\ve.north);

  % Explanatory notes share a common top edge.
  \node[note] at (\xone,-5.36) {entailed by $O$};
  \node[note] at (\xtwo,-5.36) {entailed by $O$};
  \node[note,text=figgreen] at (\xthree,-5.36)
    {in $\Gamma$, hedged\\as $\mu$ requires};
  \node[note,text=figred,text width=3.0cm] at (\xfour,-5.36)
    {contradicted by $O$;\\\textsc{contradicted}};

  % Separate outputs, figurative language, and the response-level record.
  \node[stylebox,text width=8.60cm,minimum height=0pt,anchor=north,
        font=\scriptsize]
    (stylecheck) at (-0.50,-6.05)
    {\textbf{Outside the truth contract:}
     the task also requests an engaging museum voice. Prompt--response
     style-marking alignment compares that requested level with the level the
     response realizes and reports match or mismatch separately. It creates no
     claim and changes no label.};
\end{tikzpicture}}
\caption{\textbf{From one prompt to four claim-label decisions.}
One task context, one truth contract, four claims, and three labels. The task
oracle fixes the evidence state and separates the two \textsc{entailed} claims
from the rest; permission scope and required status marking then separate the
licensed-divergence label (\LD) from the hallucination label (\Hall) on the one
claim the oracle leaves \textsc{unknown}. The claim that the decree dates from
1799 is contradicted by the oracle's date of 196 BCE and receives \Hall{}
regardless of creative permission. The requested style-marking level is
recorded below the pipeline,
outside the contract, and never creates a fourth label. The precise temple-
entrance placement and the incorrect date are constructed test claims.}
\label{fig:claim-label-annotation}
\end{figure}
\FloatBarrier

Next, the evaluator compares $q^*$ with the task oracle $O$. The excerpt neither
entails nor contradicts the placement, so the evidence state is
\textsc{unknown}. Because the oracle leaves the claim unknown, the evaluator
checks the remaining contract fields. The claim falls inside the permission
scope, which allows one marked reconstruction. Its observed status marking also
satisfies the requirement to present that reconstruction as conjecture. The
claim therefore receives the licensed-divergence label rather than the
hallucination label.

Under the strict-grounding task in Figure~\ref{fig:prompt-to-claim}, the
permission scope is empty. The same claim would therefore receive the
hallucination label with reason code \textsc{out-of-scope}, even though its
evidence state would remain \textsc{unknown}. Requested and observed writing
style are compared separately. Style neither creates nor excuses a
truth-conditional claim.

Claim labeling has two phases. The first prepares the claim. The second assigns
its label and branches exactly once.

\begin{description}
  \item[Preparation.] The evaluator selects a \emph{response span}, that is, a
        contiguous stretch of the response treated as one unit of
        interpretation, typically a clause or a sentence.
        It recovers the contextualized claims to which the span commits. Each
        claim is then canonicalized into normalized content $q^*$ and observed
        status marking $m^*$.
  \item[Decision.] The evaluator compares $q^*$ with $O$ and records its
        evidence state. For an \textsc{entailed} or \textsc{contradicted}
        claim, the evidence state determines the label, and no further check
        applies. An \textsc{unknown} claim proceeds to two checks: whether
        $q^*$ falls inside $\Gamma$ and, if it does, whether $m^*$ satisfies
        $\mu$. The evaluator then assigns the claim label and, for a
        violation, its reason code.
\end{description}

The evidence state directly settles the label for \textsc{entailed} and
\textsc{contradicted} claims. Only an \textsc{unknown} claim proceeds to the
remaining contract checks, $\Gamma$ and $\mu$.
If preparation or adjudication fails, the span remains unlabeled rather than
receiving a default label. The claim trace, which preserves the claim-level
decisions, is the primary output.
Appendix~\ref{app:diagnostics} defines the response-level record and the
procedural failures reported instead of a label.

\subsection{The Truth Contract and Task Oracle}\label{subsec:contract}

\paragraph{Truth contract}
A truth contract specifies what the task treats as established, what the
response may introduce beyond it, and how such content must be presented.
For a task context $p$, it is the triple $\TC=(O,\Gamma,\mu)$.
The three fields answer separate questions: What task-specific standard decides
the evidence state? What content may extend beyond what that standard
establishes? How must that content be marked? The contract is therefore indexed
by the complete task context rather than by the literal prompt alone.


We drop the subscript $p$ when the task context is clear.
Requested and observed style-marking levels are not truth-contract fields.
The requested level is a task variable used during claim recovery; the
observed level is a response-level measurement used in prompt--response
style-marking alignment.
Only $O$, $\Gamma$, and $\mu$ enter the claim-labeling rule.

When instructions conflict, the evaluator must state the precedence of system
and user instructions and how applicable safety, legal, and domain constraints
bound the permission scope from outside.
A user request cannot override those constraints by redefining the task oracle
or permission scope.

\paragraph{Task oracle ($O$)}
The task oracle is the standard a task uses to determine whether a claim is
entailed, contradicted, or unknown.
It specifies which information sources are admissible, their temporal scope,
their relative authority, and the procedure used to resolve conflicts.
Existing surveys use different notions of source, context, fact, and world
knowledge \citep{ji_survey_2023,bang_hallulens_2025}; reproducible evaluation
therefore requires a concrete task-oracle specification or an explicit
adjudication procedure.
A task oracle may comprise a source document, a retrieval set, a database,
gold labels, an environment or tool state, a human adjudication procedure, the
declared constraints of a fictional world, or a combination of these sources.
For example, a supplied article can instantiate the task oracle for a
source-faithfulness task, execution tests can instantiate it for a code task,
and declared story-world constraints can instantiate it for in-frame claims.

The task oracle does not represent whatever the user would like to be true.
It records what the evaluation treats as established or authoritative for the
task.
Content that the task permits beyond the oracle is represented separately by
the permission scope $\Gamma$.
An instruction to propose a hypothesis can therefore authorize the proposal
without adding that hypothesis to the task oracle or making it true.

The task oracle is idealized because it specifies the standard that the
evaluation should apply.
A concrete implementation may still fail to access, complete, or adjudicate
that standard reliably.
Appendix~\ref{app:diagnostics} records such implementation failures rather than
converting them into claim labels.

\paragraph{Permission scope ($\Gamma$)}
Return to the clinical assistant of Section~\ref{sec:intro}, now asked to list
candidate explanations of a patient's symptoms. The task lets the response
raise a possible diagnosis the record does not establish, and forbids it to
supply a drug dose the record does not contain. Both are claims the evidence
leaves unknown; only one is authorized.
$\Gamma$ records that difference. It specifies the topics, entities, claim
types, or fictional world in which evidence-unknown content may be introduced.
A factual-only task has an empty permission scope: every truth-conditional
claim must be entailed by the task oracle $O$.
Searle's account of fictional discourse provides indirect support for
distinguishing story-world commitments from assertions presented as
real-world fact \citep{searle_logical_1975}.
We operationalize that distinction through permission scope, which may
include invented characters and events while excluding claims about named
real people.

Permission scope records authorization, not value.
The check therefore asks what the task allows, not what is worth saying: a
carefully reasoned but invented drug dose still receives the hallucination
label, whereas an unhelpful but in-frame fictional invention still receives the
licensed-divergence label and a low score on whatever usefulness criterion the
task defines.
Section~\ref{subsec:creativity} situates this separation against creativity
evaluation, which scores value on its own terms.

\paragraph{Required status marking ($\mu$)}
$\mu$ specifies how the response must signal an evidence-unknown claim.
The signal may be a local hedge, a hypothesis label, a section heading, or a
task-level fictional frame.
In the clinical task above, ``this may be early Lyme disease, but serology is
required'' satisfies the required status marking, while ``this is early Lyme
disease'' does not.
The observed status marking $m^*$ is recovered from the complete response and
task context because the relevant marker may appear outside the local span.
The evaluator then compares $m^*$ with the required status marking to decide
whether the claim is marked as $\mu$ requires.

\subsection{Claim Recovery and Canonicalization}\label{subsec:unit}

We use a claim-level unit because long responses may contain multiple checkable
propositions alongside material that is not independently verifiable
\citep{min_factscore_2023,song_veriscore_2024}.
When a response span contains a commitment, interpreting it within the
complete response and task context yields one or more contextualized
truth-conditional claims.
The same wording can therefore yield different claims in fiction, metaphor, and
hypothetical language.
We reserve two terms for two different operations.
\emph{Claim extraction} refers to existing methods that segment a response into
checkable units; Section~\ref{subsec:extraction} reviews them.
\emph{Claim recovery} refers to the broader, context-sensitive step that
attributes claims to the response before canonicalization, and it is the step
this rule relies on.

Before claim verification, the evaluator canonicalizes each recovered claim
into two components, $q^*$ and $m^*$.
The component $q^*$ records normalized truth-conditional content. This content
includes entities, predicates, negation, quantities, temporal scope, and any
attribution that changes what is claimed.
The component $m^*$ records observed status marking, including relevant
modality and fictional or hypothetical framing.
This separation allows $q^*$ to be checked against the task oracle and
permission scope without discarding the presentation information needed by
$\mu$.

Claim recovery follows three rules.
First, commitments that can receive different evidence states are recovered
as separate claims.
Second, attribution, negation, modality, temporal scope, and discourse frame
are preserved in $q^*$ or $m^*$ as appropriate.
Third, metaphorical wording does not create a separate claim unless the
response makes an attributable truth-conditional commitment.

Claim recovery can fail in two directions.
\emph{Under-decomposition} merges commitments that could receive different
evidence states; \emph{over-decomposition} splits one commitment into several
redundant subclaims.
Over-decomposition distorts claim counts, coverage estimates, and any
response-level aggregation based on label frequency: for an \textsc{unknown}
permitted commitment, it inflates the number of \LD-labeled claims without
adding new content.
Evaluation should therefore measure both directions against an independently
adjudicated claim set.

Some spans yield no truth-conditional claim at all.
Such a span receives no claim label, and the absence of a label is a normal
outcome rather than one of the failures defined in
Appendix~\ref{app:diagnostics}.
Figurative language is interpreted in the complete response and task context
rather than literalized automatically.
Every truth-conditional commitment recovered from the span is canonicalized
before claim verification.

We use \emph{claim verification} for the step that compares the canonical
claim content $q^*$ with the task oracle $O$ and, when adjudication succeeds,
assigns exactly one evidence state: \textsc{entailed},
\textsc{contradicted}, or \textsc{unknown}.
Claim verification therefore follows claim recovery and canonicalization but
precedes the permission-scope and required-status-marking checks.

A pair of matched realizations instantiates \emph{claim-preserving style
variation} when both yield the same canonical claims under a fixed task oracle
$O$, permission scope $\Gamma$, and required status marking $\mu$.
Only their surface realization and requested or observed style-marking levels
may differ.
Appendix~\ref{app:diagnostics} gives the formal stability test.

Recovering each claim correctly does not establish that the response combines
those claims through a valid inference.
Claim recovery and global response verification are separate evaluation
problems, and the scope limit stated in Section~\ref{sec:intro} applies here:
a response whose claims are individually well labeled may still reason badly
from them.

\subsection{From Evidence State to Claim Label}\label{subsec:rule}

Once a stable canonical claim has been obtained and the three contract fields
are fixed, the evaluator attempts claim verification against $O$.
When adjudication succeeds, claim labeling applies the rule below.
That rule is not uniform across evidence states.
For an \textsc{entailed} or a \textsc{contradicted} claim, the evidence state
alone determines the label.
For an \textsc{unknown} claim it does not.
The permission-scope and required-status-marking checks then decide.
We reserve \textsc{entailed}, \textsc{contradicted}, and \textsc{unknown} for
evidence states.
The three claim labels are grounded, hallucination, and licensed divergence.
We abbreviate them as \Grd, \Hall, and \LD\ in the rule below, in figures, and
in tables, and use the full names in running text.

The two vocabularies are not redundant.
An evidence state describes the relation between a claim and the task oracle; a
claim label describes the standing of that claim under the task.
Two of the three evidence states map onto a label one to one, but the
hallucination label does not map back: it is reached from
\textsc{contradicted}, and also from \textsc{unknown} by two different routes.
That is why a claim label carries a reason code and an evidence state does not.
We write \emph{grounded} rather than \emph{supported} for this label because
fact-verification resources such as FEVER \citep{thorne_fever_2018} use
\textsc{supported} for an evidence state, namely for what we call
\textsc{entailed}.
Reusing that word across the two vocabularies would blur the very distinction
the rule depends on.

For each canonical claim, the evaluator first records whether $O$ entails,
contradicts, or leaves $q^*$ unknown.
We call a claim with evidence state \textsc{unknown} an
\emph{\textup{\textsc{unknown}} claim}.

The claim label then follows three rules:
\begin{description}
  \item[\Grd.] The task oracle entails $q^*$.
  \item[\Hall.] The task oracle contradicts $q^*$, or it leaves $q^*$
        unknown and the claim falls outside $\Gamma$ or fails the required
        status marking $\mu$. The evaluator records exactly one reason code,
        applying the following precedence:
        \begin{enumerate}
          \item if $O$ contradicts $q^*$, record \textsc{contradicted};
          \item otherwise, if the evidence state is \textsc{unknown} and $q^*$
                falls outside $\Gamma$, record \textsc{out-of-scope};
          \item otherwise, if $q^*$ lies inside $\Gamma$ but $m^*$ fails
                $\mu$, record \textsc{marking-failure}.
        \end{enumerate}
  \item[\LD.] The task oracle leaves $q^*$ unknown, the claim lies
        inside $\Gamma$, and $m^*$ satisfies $\mu$.
\end{description}
The licensed-divergence label records contract compliance and authorization
under the task, not established truth, usefulness, overall response quality, or
safety. A contradicted claim therefore receives the hallucination label once
the procedural prerequisites are satisfied. In fiction or counterfactual tasks,
the declared frame is part of the context used to recover the claim and specify
$O$; an in-frame invention is not automatically a real-world assertion. An
entailed claim that violates a separate style instruction remains grounded,
while the style or instruction-following evaluation records the mismatch.

The hallucination label is the single top-level label for every truth-contract
violation. The kind of violation is recorded by the mandatory reason code, not
by a separate label.

A claim that lies inside the permission scope but fails the required status
marking therefore receives the hallucination label with reason code
\textsc{marking-failure}. The task authorized the content only under a marking
the response did not supply. Presented as established rather than as a
hypothesis, the claim asserts more than the contract permits.

Splitting these cases into separate top-level labels would obscure that all
three are failures of the same contract, whereas the reason code preserves the
distinction for diagnosis and mitigation.
The reason code identifies the first contract condition that fails, so a claim
that is both outside $\Gamma$ and asserted without the required hedge
receives \textsc{out-of-scope}: the marking check applies only to claims the
task permits.

An explicit refusal or a statement such as ``the available evidence is
insufficient'' is evaluated as a meta-claim about the evidence. It is not
automatically treated as asserting the embedded proposition. Treating the
refusal or insufficiency statement as a meta-claim prevents calibrated
abstention from being mislabeled as hallucination.

\FloatBarrier

\subsection{Style as a Separate Robustness Variable}\label{subsec:style}

Style-marking level is a separate robustness variable: it may affect claim
recovery, but it never changes truth, evidence state, permission scope, or a
claim label. Required status marking concerns the epistemic or discourse status
of an \textsc{unknown} claim; style marking concerns rhetorical form.

By \emph{style marking} we mean the degree to which the wording of a response
departs from plain expository prose.
Plain prose is minimally marked; sustained metaphor or a strong authorial voice
is heavily marked, and stylistics describes such departures as foregrounding
\citep{leech_short_style_1981,vanpeer_stylistics_2020}.
The departure may be lexical, syntactic, phonological, or discourse-level, and
it may be produced by devices such as deviation, parallelism, repetition, or
metaphor.
Style marking is a matter of degree; it is not the identity of a named style,
and it is not evidence of factual error.
Case~E in Section~\ref{sec:cases} gives a minimal pair: two realizations of one
canonical claim, one minimally marked and one marked.

The requested style-marking level records the degree of style marking the task
asks for. It is a separate task variable available as contextual input during
claim recovery, and it takes one of three ordered values:
\begin{itemize}
  \item \emph{minimally marked}: plain expository prose, with minimal rhetorical transformation and no salient authorial voice;
  \item \emph{locally marked}: local metaphor, register shift, or a light stylistic voice without sustained transformation of the response;
  \item \emph{sustained marked style}: sustained figurative language, a salient voice, or substantial stylistic transformation.
\end{itemize}
The levels rank only the degree of style marking. They do not represent the
identity of a named style, creativity, factuality, or permission scope.
Where a study needs to index the three values, we write
$\sigma\in\{0,1,2\}$; Section~\ref{sec:agenda} and
Appendix~\ref{app:diagnostics} use that indexing to state factorial designs.

Because style is multi-dimensional, the three levels form a working
discretization rather than a validated universal scale
\citep{kang_style_2021,jin_deep_2022}.
The boundaries, especially between levels 1 and 2, require empirical
validation.

The requested level is specified by the task; the observed level is realized
by the response. Their \emph{prompt--response style-marking alignment} is
\textsc{match}, \textsc{mismatch}, or \textsc{not applicable} when the task
specifies no target. This provisional categorical rule may later be replaced
by ordinal distance or tolerance. If either level cannot be adjudicated
reliably, the component is withheld and a procedural failure is reported.
Style-marking alignment is neither a claim label, a creativity score, nor a
measure of model alignment.

Requested style-marking level is available as contextual input during claim
recovery, and its effect is tested rather than assumed.
Every recovered claim must remain warranted by linguistic evidence in the
produced response, and the observed level records the marking actually
produced.

Existing evaluators use claim decomposition, natural-language inference, or
semantic similarity, while model-based judges may prioritize style or apply a
narrower subset of human criteria
\citep{min_factscore_2023,chen_menli_2023,aynetdinov_semscore_2024,feuer_style_2025,lyu_converge_2026}.
The cited work does not hold a canonical claim fixed while wording varies;
whether current pipelines recover the same claims across matched style variants
remains an open empirical question.

\section{Related Work}\label{sec:related}

Each neighboring tradition contributes one part of the broader evaluation, but
none makes the claim-labeling decision proposed here.
Factuality and source-faithfulness evaluation relate claims to evidence.
Claim-level evaluation defines the unit of analysis.
Instruction-following evaluation represents task constraints.
Creativity research separately evaluates novelty, usefulness, and response
quality.
The truth contract addresses the remaining question for a claim that the task
oracle leaves \textsc{unknown}.
For such a claim, permission scope and required status marking determine whether
the label is licensed divergence or hallucination.

\subsection{Hallucination Taxonomies}

Hallucination surveys classify the phenomenon along several related but
non-equivalent axes.
\citet{ji_survey_2023} distinguish intrinsic hallucinations from extrinsic
hallucinations. Intrinsic hallucinations contradict the source; extrinsic
hallucinations cannot be verified from it.
\citet{huang_survey_2025} separate factuality hallucinations from faithfulness
hallucinations. Factuality hallucinations involve factual contradiction or
fabrication, whereas faithfulness hallucinations involve instruction, context,
or logical inconsistency.
\citet{zhang_sirens_2025} instead distinguish input-, context-, and fact-conflicting outputs.
Other reviews organize causes across the model lifecycle or examine factuality and faithfulness metrics for open-ended generation \citep{wang_factuality_survey_2024,malin_faithfulness_review_2025,lamba_lifecycle_2026}.
From a creativity perspective, \citet{jiang_survey_2024} distinguish divergent generation from convergent evaluation.

These surveys answer a descriptive question: what kind of mismatch occurred?
They classify mismatches by evidence relation, inconsistency type, source,
cause, or evaluation setting.
The truth contract asks a later question.
If the task oracle leaves a claim \textsc{unknown}, does the claim fall inside
$\Gamma$, and does its observed status marking satisfy $\mu$?
The resulting claim label therefore remains distinct from the descriptive
taxonomy.

\subsection{Task Oracles in Factuality and Faithfulness Evaluation}

Source-faithfulness evaluation asks whether a claim is supported by a
supplied source document, whereas factuality evaluation checks it against
external knowledge \citep{maynez_faithfulness_2020,kryscinski_evaluating_2020}.
In source-faithfulness evaluation, the supplied document serves as the task
oracle. In factuality evaluation, external knowledge sources or human
adjudication may serve that role.
The chosen oracle assigns the evidence state: entailed, contradicted, or
unknown. Only an \textsc{unknown} claim then proceeds to the permission-scope
and required-status-marking checks.

\subsection{Claim Extraction, Recovery, and Commitment}\label{subsec:extraction}

Specifying the task oracle does not yet determine which propositions the
response commits to. Evaluation must first recover claims in context.
Assertion is treated here as a public commitment to propositional content
\citep{stalnaker_assertion_1978}.
Because interpretation depends on conversational context, claim recovery
retains the context needed to attribute that commitment
\citep{clark_using_1996}.
Canonicalization may add context needed to make a claim stand alone, but it
must preserve the original meaning \citep{choi_decontextualization_2021}.
Hedges can qualify categorical commitment \citep{hyland_hedging_1996}.
Their observed contribution is recorded in $m^*$.
These sources motivate contextualized claims and canonicalization; they do not
validate the proposed categories of required status marking.

Operationalizing contextual commitment requires reliable units of
verification. External claim-extraction methods decompose responses into
checkable units before verification
\citep{min_factscore_2023,song_veriscore_2024,metropolitansky_claimify_2025}.
Some verification pipelines then allocate more effort to claims with greater
uncertainty \citep{hassan_factor_2026}.
Our rule addresses a different question: it varies the claim label with
permission scope, not the amount of verification effort.

These stages can fail in different ways.
Coverage errors omit attributable commitments, whereas atomicity errors merge
propositions that could receive different evidence states
\citep{metropolitansky_claimify_2025,wanner_closer_2024}.
Decontextualization can remove attribution, modality, or discourse framing,
and canonicalization can alter $q^*$ or $m^*$
\citep{wanner_closer_2024,choi_decontextualization_2021}.
These methods provide machinery for claim extraction.
Following Section~\ref{subsec:unit}, we evaluate claim recovery and
canonicalization separately before assigning claim labels.

\subsection{Instruction Following, Permission, and Status Marking}

Claim extraction determines what is being evaluated, but not what the task
allowed. Instruction-following and intent-aware evaluation address this second
question and are therefore the closest paradigms to our proposal.
The key distinction is between a truth contract and a well-specified
instruction-following rubric.
This subsection explains what that literature measures and which claim-labeling
decision it does not make.

Instruction-following evaluation asks whether a response satisfies the
constraints the task expresses, and its benchmarks turn those constraints into
automatically checkable requirements on the output
\citep{zhou_ifeval_2023}.
More recent frameworks decompose a constraint along several dimensions rather
than treating constraint type as the only variable
\citep{ye_muldimif_2025}.
Intent-aware evaluation adds a diagnosis of query conditions the response
omitted or misread \citep{hao_faithqa_2025}.
Work that studies instruction following and source faithfulness jointly finds
that the two objectives can pull apart and require explicit reconciliation
\citep{wu_dancing_2024}.
Together, this literature provides methods for recovering task constraints from
context. The truth contract presupposes that these constraints can be recovered
reliably; it does not itself provide that recovery method.

Two properties of that literature bear on our proposal.
First, a constraint in these benchmarks specifies what a response must do and
is checked after generation. Permission scope instead specifies what a response
may do. It authorizes evidence-unknown content but does not require the response
to produce any. A response that produces no such content therefore cannot
violate the permission scope.
Because $\mu$ constrains how content is presented, an instruction-following
rubric represents it more naturally than $\Gamma$.

Second, judging at constraint level is not the same as judging the response as
a whole. \citet{lee_mcjudgebench_2026} show that LLM judges are usually assessed
only through overall-response verdicts. They instead evaluate the judges
against per-constraint labels.
This distinction mirrors the one this paper draws between a claim label and a
response-level record: an aggregate judgment hides which requirement produced
it.

A sufficiently detailed instruction-following rubric could reproduce our
labels. To do so, it would need to represent the task oracle, permission scope,
and required status marking explicitly and apply them in the specified order.
Such a rubric would instantiate the rule rather than replace it.
The rule contributes the proposal that hallucination judgments require these
three fields. For an \textsc{unknown} claim, permission scope and required
status marking---not further evidence---determine the label.

Two boundaries follow. Permission scope does not measure usefulness, and
required status marking is not a general style score.
Safety, legal, and domain-specific constraints impose external limits on
$\Gamma$ and remain separate assessments. A task context cannot extend the
permission scope beyond those limits.

\subsection{Creativity and Response-Level Quality}\label{subsec:creativity}

The previous paradigms ask what a task requires.
Creativity research asks a question that the truth contract deliberately does
not encode: whether the content is valuable or effective.
This subsection explains why we keep the two questions separate.

Creativity research has long defined a creative product as one that is both
novel and useful or appropriate \citep{runco_standard_2012}.
The two criteria are not interchangeable and are not equally weighted.
\citet{diedrich_are_2015} report that usefulness predicts rated creativity only
among highly novel ideas. In their analysis, novelty is therefore a first-order
criterion and usefulness a second-order criterion.
\citet{pichot_reflective_2024} find the two components weakly or negatively
correlated in judged ideas. They argue that a single composite score therefore
misrepresents the standard two-part definition.
Automatic judges are also least reliable on the value dimension.
LLM creativity evaluators converge with human raters on novelty but diverge on
the contextual criteria that carry value \citep{lyu_converge_2026}.
We take from this literature the practice of keeping novelty, usefulness, and
quality as separately reported criteria, and the caution that a single
creativity score hides which criterion produced it.

A parallel separation appears in factuality research.
Instruction tuning can make responses more informative while reducing their
truthfulness \citep{wu_balancing_2025}.
Preference learning can also reward fluent, helpful wording over accurate
content. Recent work therefore treats factuality and informativeness as a
controllable trade-off rather than a single objective
\citep{gong_factuality_2026}.
Usefulness and factual support therefore come apart empirically, not only
conceptually.

Neither creativity evaluation nor factuality--informativeness research supplies
the decision our rule makes.
Value is a property of the content that an evaluator scores after the fact.
Permission scope is a property of the task that must be fixed before the
response is read.
Section~\ref{subsec:contract} states that ordering, and the minimal pair given
there shows why it matters: an ingenious fabrication remains a hallucination
and a dull in-frame invention remains licensed divergence.

Recent proposals distinguish potentially valuable or ``intelligent''
hallucinations from defective ones
\citep{jiang_survey_2024,sui_confabulation_2024,yang_hicbench_2025}.
Other work examines bounded confabulation for archival gap-filling
\citep{sui_critical_2025} or distinguishes pedagogical reformulation from
factual error in AI-generated science stories
\citep{argese_storyscore_2026}.
These approaches evaluate the value of divergence.
The truth contract asks a prior question: did the task authorize that
divergence?

Creativity and overall response quality therefore require task-specific
criteria beyond claim labels.
Novelty, usefulness, preference, and quality may contribute to a creative-task
assessment. None of them, however, makes an unsupported proposition true or
expands permission scope.
A response can satisfy the truth contract and still perform poorly on the
task's creativity or quality rubric. Conversely, a highly rated response can
still contain a contract-violating claim.
These diagnostics should remain visible rather than being collapsed into the
claim label.

\subsection{Global Reasoning and Complete-Response Evaluation}\label{subsec:global}

The paradigms above evaluate claims one at a time, and so does our rule.
A separate body of work asks what claim-level evaluation misses when the claims
are considered together. That literature defines an important boundary of our
scope.

\citet{yan_logicscore_2026} call this limitation \emph{attribution myopia}:
evaluators verify isolated statements and their attributions but overlook the
global logic of a long-form answer. An answer can therefore be factually
grounded yet logically incoherent.
The same limitation applies to a claim trace. Prior work identifies three
relevant failure modes.

First, individually acceptable claims may be connected by unsound reasoning.
Work on chain-of-thought faithfulness shows that a model's stated reasoning need
not reflect the process that produced its answer
\citep{arcuschin_cot_2025}. A correct final claim therefore does not establish
that the preceding steps were sound.
Step-level benchmarks make the same point from the evaluation side.
Methods that detect problems at the response level cannot identify where a
chain failed or what kind of failure occurred \citep{pham_grace_2026}.
Our claim trace localizes a violation to a claim and a contract field.
That unit is finer than a response-level score but is still not an inference
step.

Second, claims may be individually adjudicable and mutually inconsistent.
\citet{mundler_selfcontradictory_2024} show that a model can assert two
contradictory sentences within one context.
Detecting this failure requires comparing generated statements with one another
rather than comparing each statement with a source.
A truth contract compares every claim with $O$ but does not compare claims with
one another. A response whose claims are each entailed can therefore remain
internally contradictory without any claim receiving \Hall.

Third, a response may be accurate and incomplete.
Long-form factuality evaluation has largely measured precision over extracted
claims, but recent work argues that recall matters as much.
\citet{liu_verifact_2025} report that high precision does not imply high coverage
of the facts a response should contain. \citet{jafari_recall_2026} weight recall
by the importance of the omitted material.
Claim-recovery coverage, defined in
Appendix~\ref{app:diagnostics}, measures whether the evaluator found the claims
a response \emph{made}; it does not measure whether the response made the claims
the task \emph{required}.

Complete-response evaluation must combine the claim trace with separate checks
for reasoning, coherence, coverage, relevance, and task-specific success.
These checks do not override claim labels: a favorable response-level judgment
cannot retroactively authorize a claim that violates the truth contract.
Section~\ref{sec:agenda} treats the composition of these diagnostics as an open
research problem rather than a solved one.

\subsection{Claim-Preserving Style Variation}
The paradigms reviewed so far concern what a claim commits to and what a task
permits. A final body of work examines how the same commitment can be worded in
different ways.
Stylistics and style-transfer research examine changes in linguistic form
intended to preserve content
\citep{leech_short_style_1981,vanpeer_stylistics_2020,pavlick_empirical_2016,rao_dear_2018,briakou_evaluating_2021}.
That literature measures whether content survives a change of form, but it does
not ask whether an evaluator still recovers the same claims from the changed
form.
Hallucination surveys and benchmarks instead organize evidence relations such
as support, contradiction, or unverifiability
\citep{ji_survey_2023,huang_survey_2025,bang_hallulens_2025}.
Style-transfer research varies form while aiming to preserve content;
hallucination benchmarks take the reverse approach by keeping form fixed and
varying the evidence relation.
Neither tradition therefore tests whether an evaluator recovers the same
canonical claim from matched style variants.
That gap motivates the controlled test defined in
Section~\ref{sec:framework} and the resource analysis reported below.

\section{Resource Mapping}\label{sec:methodology}

Section~\ref{sec:related} argued that the traditions reviewed do not separately
represent the task oracle, permission scope, and required status marking.
We examine that diagnosis across the selected resources.
We code forty-one evaluation resources for whether their designs represent each
of the three contract fields. We also record whether they vary requested style
marking while holding canonical claim content fixed, and whether they compare
recovered claims or claim labels across those variants.
The coding is descriptive. The next subsection states what it can and cannot
support before reporting the results.

\subsection{Resource Selection and Scope}

The evaluation resource is the unit of analysis in this purposive mapping.
We use \emph{evaluation resource} as an umbrella term for a benchmark, dataset,
or evaluation protocol with an identifiable evaluation design.
FEVER enters the mapping as a benchmark and dataset, whereas WritingBench enters
as a benchmark with an associated evaluation protocol.
We purposively selected forty-one resources spanning the design space relevant
to our argument: factual question answering, grounded generation, intent
evaluation, creative writing, divergent thinking, and constrained problem
solving.

Purposive selection allows deliberate coverage of the relevant design space.
It can identify combinations absent from the selected resources, but it cannot
establish that they never occur elsewhere or estimate their prevalence.
% TODO(authors): state in one clause how the forty-one resources were located
% (e.g. from the surveys cited in Section 3 and their citation neighbourhoods).
% Reviewers will ask; we cannot report a search procedure we have not recorded.
Each resource was coded by the authors against the closed vocabulary reported
in Appendix~\ref{app:mapping}, and the resource-level coding is published in
full so that every descriptive claim below can be checked against the
underlying evidence.
The mapping describes which fields the selected evaluation designs do and do
not represent. It also identifies the combination that motivates our framework.
Because the sample is purposive, the mapping does not validate the framework,
demonstrate a performance improvement, or estimate prevalence in the wider
literature.

\subsection{Coding Protocol}

A \emph{nominal task profile} is a categorical label that we assign to each
resource to summarize the kind of task it evaluates.
These profiles are descriptive groupings introduced for this mapping.
They are not claimed by the original resources, they are not ordered, and they
never enter the claim-labeling rule.

For each resource, we recorded seven questions:
\begin{enumerate}
  \item What task-oracle type $O$, if any, is available for adjudicating a
        claim's evidence state?
  \item What quality, preference, novelty, or task-success criterion, if any,
        is used separately from the task oracle?
  \item Does the protocol control or score requested style-marking level
        $\sigma$ while holding target claim content fixed?
  \item Which nominal task profile best describes it: strict grounding, intent
        evaluation, declared-frame invention and ideation, constrained solution
        search, or heterogeneous suites and tool use?
  \item How does the protocol represent permission scope $\Gamma$?
  \item Does it score required status marking $\mu$ separately?
  \item When requested style marking varies while target claim content remains
        fixed, does the protocol compare the recovered claims or their labels
        across variants?
\end{enumerate}

The original resources do not necessarily use the term \emph{task oracle}.
The coding therefore records our interpretation of the adjudication standard
available in each evaluation design.
For task-oracle type, the closed vocabulary is source document, retrieval set,
database or knowledge base, gold labels, environment or tool state, human
adjudication, fictional-world constraints, mixed or task-dependent, and none
identifiable.

FEVER, for example, is coded as strict grounding because its claims are
verified against Wikipedia, which instantiates its task oracle, and no
unattested content is authorized. LitBench is coded as declared-frame
invention and ideation because it evaluates stories written under a story
frame; its human-preference signal evaluates response quality rather than
claim truth. ToolQA is coded as heterogeneous suites and tool use because its
tasks draw on fixed external data sources and programmatically generated gold
answers, yielding a mixed or task-dependent oracle.

Appendix~\ref{app:mapping} reports the resource-level coding using a closed
vocabulary defined before the tables.
The task oracle is kept separate from quality, preference, novelty,
feasibility, correctness, and general task-success criteria. Such criteria are
not treated as $O$ unless the protocol uses them to establish or contradict the
claim being adjudicated.

\subsection{What the Coding Shows}\label{sec:mapping}

The complete coding of the selected sample appears in Appendix
Tables~\ref{tab:full-mapping-factual-a}--\ref{tab:full-mapping-creative}.
The tables record whether each field is separately represented in an evaluation
design, not whether the protocol manipulates that field experimentally.
We report the three contract fields in turn, then the treatment of requested
style-marking level.

\paragraph{Task oracle}
Thirty-five of the forty-one selected resources make an adjudication standard
available, and our coding assigns each standard a type.
The most common types are mixed or task-dependent (fifteen resources), source
document (eight), and gold labels (six).
The remaining types are environment or tool state (three), retrieval set (one),
structured knowledge base (one), and specified human procedure (one).
The other six resources are coded \emph{none identifiable} because their
protocols provide no standard that can establish or contradict response claims.
All six belong to the declared-frame invention and ideation profile.
Thus, in the selected sample, the task oracle is the only contract field that
most resources already represent separately.

Quality, preference, novelty, feasibility, and correctness criteria are coded
separately throughout.
For example, the five constrained solution search resources use feasibility,
tests, or specifications as task-success criteria. Their protocols do not use
those criteria to establish or contradict an evaluated claim.

\paragraph{Permission scope}
No selected resource declares a permission scope.
In all forty-one cases, our coding had to infer the scope from the task rather
than read it from the protocol. The inferred values fall into three groups.
All twenty-two resources of the strict grounding profile are coded
\emph{inferred empty}: the design admits no evidence-unknown content, but no
part of the protocol says so.
Among the declared-frame resources, eight are coded \emph{implicit frame} and
one is coded \emph{partial}. A story, task, or ideation frame licenses
invention, but the design does not state its boundary. Nothing in the protocol
therefore separates an in-frame invention from an out-of-frame one.
The remaining ten are coded \emph{not separately represented}: the
intent-evaluation resource, four resources in heterogeneous suites and tool use,
and five constrained solution search resources.

\paragraph{Required status marking}
No selected resource scores required status marking as a separate dimension.
Twenty-six are coded \emph{not scored}, and fifteen \emph{not separated},
meaning that a relevant marking criterion is absorbed into a broader task or
rubric rather than assessed on its own.
Nothing in the selected designs therefore distinguishes a permitted claim that
was hedged as the task required from the same claim asserted as established.
AA-Omniscience is the closest case. It explicitly permits abstention but folds
abstention into its reliability metrics rather than representing it as licensed
evidence-unknown content
\citep{jackson_aaomniscience_2025}.

\paragraph{Requested style-marking level}
One resource, WritingBench \citep{wu_writingbench_2025}, scores a requested
style constraint; the other forty leave the requested style-marking level
uncontrolled.
No selected resource, including WritingBench, holds canonical claim content
fixed while manipulating that level.
Question~7 is therefore uniformly \emph{not applicable} in the selected sample
and is not repeated as a separate column in Appendix~\ref{app:mapping}.
General writing quality, creativity, or linguistic complexity is evaluated in
several creative-writing resources, but scoring those properties is not the
same as controlling the requested level with the claim held constant.
Consequently, the selected factuality and faithfulness resources do not test
whether claim-preserving style variation changes claim recovery or claim
labels. Whether their verifiers are sensitive to such variation remains an open
question.

\paragraph{The missing combination}
In our selected sample, no resource separately represents the task oracle,
permission scope, and required status marking while also controlling
claim-preserving style variation.
This absence is not a low value on a scale. It is a combination of design
features that no selected protocol implements.
Figure~\ref{fig:profiles} summarizes the pattern by nominal task profile and
treatment of requested style-marking level.

\begin{figure}[!ht]
\centering
\resizebox{\textwidth}{!}{%
\begin{tikzpicture}[
    x=1cm,y=1cm,
    >=Latex,
    font=\small,
    axis/.style={thick,-{Latex[length=3mm]}},
    clusterbox/.style={
        rounded corners=6pt,
        thick,
        align=left,
        inner sep=6pt,
        text width=3.55cm
    },
    note/.style={font=\scriptsize,align=left}
]

% Schematic axes: vertical offsets are retained only for legibility.
% Horizontal grouping only: no direction and no metric interpretation.
\draw[thick] (0,0) -- (14.8,0);
\node[below,align=center] at (7.4,-0.30)
  {Nominal task profiles
   (categorical; horizontal distance has no metric meaning)};
\draw[axis] (0,0) -- (0,8.4);
\node[anchor=west,font=\footnotesize] at (0.25,8.15)
  {Schematic treatment of requested style-marking level};

\node[left,align=right] at (-0.22,1.35)
  {\footnotesize not controlled or\\[-1mm]\footnotesize not applicable};
\node[left,align=right] at (-0.22,4.45)
  {\footnotesize vertical offsets for\\[-1mm]\footnotesize legibility only};
\node[left,align=right] at (-0.22,7.15)
  {\footnotesize scored case and missing\\[-1mm]\footnotesize claim-controlled design};
\draw[densely dotted,gray!35] (0.12,3.15) -- (14.55,3.15);
\draw[densely dotted,gray!35] (0.12,5.65) -- (14.55,5.65);

% Missing combination targeted by the proposed benchmark.
\node[
  draw=red!70,very thick,rounded corners=8pt,fill=red!6,
  text width=4.25cm,minimum height=1.65cm,align=center
] (gap) at (2.65,6.85)
  {\textbf{No selected resource}\\[1mm]
   \footnotesize strict-grounding profile\\
   \footnotesize + claim-controlled style variation};

% The only selected protocol that explicitly scores a requested style
% constraint, without holding canonical claim content fixed.
\node[clusterbox,draw=magenta!70!black,fill=magenta!12,
  text width=3.85cm] (writing) at (10.35,6.45) {
  {\bfseries Declared-frame invention and ideation}\\[1mm]
  \footnotesize style scored, not claim-controlled\\
  \footnotesize \textit{WritingBench}, row 31
};

% Resources whose requested style-marking level is not controlled.
\node[clusterbox,draw=blue!70!black,fill=blue!12,
  text width=4.15cm] (strict) at (2.35,1.15) {
  {\bfseries Strict grounding}\\[1mm]
  \footnotesize factual QA, summarization, faithfulness\\
  \footnotesize \textit{FEVER, TruthfulQA, XSum, AA-Omniscience}\\
  \footnotesize Appendix rows 1--22
};

\node[clusterbox,draw=violet!70!black,fill=violet!12,
  text width=3.15cm] (intent) at (5.50,3.70) {
  {\bfseries Intent evaluation}\\[1mm]
  \footnotesize \textit{FaithQA}, row 23
};

\node[clusterbox,draw=gray!75!black,fill=gray!12,
  text width=3.65cm] (mixed) at (6.35,0.92) {
  {\bfseries Heterogeneous suites and tool use}\\[1mm]
  \footnotesize \textit{BIG-Bench, BBH, ToolQA, HIC-Bench}\\
  \footnotesize rows 24--27
};

\node[clusterbox,draw=orange!85!black,fill=orange!15,
  text width=4.30cm] (creative) at (9.75,3.55) {
  {\bfseries Declared-frame invention and ideation}\\[1mm]
  \footnotesize \textit{CreativityPrism, LitBench, Short Story,}\\
  \footnotesize \textit{Art-or-Artifice, Automated DT, Verbal}\\
  \footnotesize \textit{Creativity, Automated Creativity,}\\
  \footnotesize \textit{Hallucinating LLM Could Be Creative}, rows 28--30, 32--36
};

\node[clusterbox,draw=green!55!black,fill=green!12,
  text width=4.15cm] (solution) at (12.35,1.08) {
  {\bfseries Constrained solution search}\\[1mm]
  \footnotesize \textit{MacGyver, EscapeBench, CreativEval,}\\
  \footnotesize \textit{NeoCoder, CreativeMath}, rows 37--41
};

% Vertical link: the empty region sits directly above the strict grounding
% cluster, in the same profile column.
\draw[densely dashed,red!55,line width=0.7pt]
  (gap.south) -- (gap.south |- strict.north);

\node[note,text width=13.5cm] at (7.40,-1.62) {
\textbf{Reading guide.}
The horizontal placement groups nominal task profiles; it is not an ordinal
permission scale.
Vertical offsets prevent overlap and do not define ordered categories; the box
text reports each resource group's treatment of requested style marking.
Only WritingBench scores requested style, and no selected resource holds
canonical claim content fixed while manipulating it.
Task-oracle type is reported in Appendix~\ref{app:mapping} and is not encoded
by horizontal distance.
Box color indicates the nominal task profile only.
The dashed line connects the strict grounding resources to the empty region
directly above them, in the same profile column.
Row numbers identify the resources in
Tables~\ref{tab:full-mapping-factual-a}--\ref{tab:full-mapping-creative}.
};
\end{tikzpicture}%
}

\caption{\textbf{Selected resources by nominal task profile and treatment of
requested style-marking level.}
Horizontal placement groups nominal task profiles, while vertical offsets are
schematic and prevent overlap; the box text reports treatment of requested
style marking.
WritingBench is the only selected resource that scores requested style, but it
does not hold canonical claim content fixed. The remaining resources do not
control requested style for a claim-preserving comparison. No selected resource
manipulates it while holding canonical claim content fixed.
The empty region at the top left highlights this absence for strict grounding,
and the dashed line links it to the strict grounding resources below.
In our selected sample, no resource separately represents the task oracle,
permission scope, and required status marking while also controlling
claim-preserving style variation.
This broader result is established by the coding in
Appendix~\ref{app:mapping}, not by the geometry of this figure.
Box color indicates the nominal task profile only.
Positions, box sizes, colors, and overlaps do not encode measured distance,
prevalence, model performance, or an ordinal permission scale.}
\label{fig:profiles}
\end{figure}
\FloatBarrier

These results identify a design gap but do not show how the rule behaves at
its boundaries. The worked cases below make the predicted outcomes explicit,
including both label changes and label stability.

\section{Worked Cases}\label{sec:cases}

The five comparisons are constructed tests of the labeling rule. Each one
states what remains fixed, what changes, and whether the rule predicts a label
change or label stability. They are not sampled model outputs and therefore do
not establish empirical superiority, annotation reliability, or empirical
coverage. Case~A
instantiates a distinction documented in summarization; Cases~B--E probe
additional boundary conditions.

Figure~\ref{fig:case-transitions} summarizes the five comparisons before they
are developed below.

\begin{figure}[!ht]
\centering
\resizebox{\textwidth}{!}{%
\begin{tikzpicture}[x=1cm,y=1cm,font=\small,
  every node/.style={inner sep=0pt}]
  \definecolor{caseblue}{RGB}{31,67,140}
  \definecolor{casegreen}{RGB}{42,118,67}
  \definecolor{casered}{RGB}{162,54,42}
  \tikzset{
    colhead/.style={font=\scriptsize\sffamily\bfseries,text=black!65,
      align=center},
    caselabel/.style={font=\small\sffamily\bfseries,text=caseblue,
      align=center},
    fixed/.style={rectangle,rounded corners=3pt,draw=black!40,fill=gray!8,
      text width=2.75cm,minimum height=1.25cm,align=left,
      font=\scriptsize,inner sep=5pt},
    condition/.style={rectangle,rounded corners=3pt,draw=caseblue!60,
      fill=caseblue!5,text width=2.75cm,minimum height=1.25cm,
      align=left,font=\scriptsize,inner sep=5pt},
    outcome/.style={rectangle,rounded corners=3pt,draw=black!45,fill=white,
      text width=3.55cm,minimum height=1.25cm,align=left,
      font=\scriptsize,inner sep=5pt},
    transition/.style={-{Stealth[length=4pt,width=3pt]},semithick,
      draw=black!60},
    transitionlabel/.style={font=\tiny\sffamily,text=black!65,
      align=center,text width=1.20cm,fill=white,inner sep=1pt},
    beforeafter/.style={font=\tiny\sffamily\bfseries,text=black!55,
      align=center}
  }

  \node[colhead] at (0,0.90) {CASE};
  \node[colhead] at (2.00,0.90) {HELD FIXED};
  \node[colhead] at (5.70,0.90) {BEFORE};
  \node[colhead] at (10.00,0.90) {AFTER};
  \node[colhead] at (14.55,0.90) {RESULT};

  % Case A
  \node[caselabel] at (0,0) {A};
  \node[fixed] (a-fixed) at (2.00,0)
    {Response span; canonical claim; $\Gamma=\varnothing$};
  \node[condition] (a-before) at (5.70,0)
    {Task oracle $O$:\\source document};
  \node[condition] (a-after) at (10.00,0)
    {Task oracle $O$:\\external evidence};
  \draw[transition] (a-before.east) --
    node[transitionlabel,above] {change $O$} (a-after.west);
  \node[outcome] (a-result) at (14.55,0)
    {\textbf{Evidence:} \textsc{unknown} $\to$ \textsc{entailed}\\
     \textbf{Label:} \textcolor{casered}{\Hall{} (\textsc{out-of-scope})}
     $\to$ \Grd};
  \draw[transition] (a-after.east) --
    node[transitionlabel,above] {evaluate} (a-result.west);

  % Case B
  \node[caselabel] at (0,-1.55) {B};
  \node[fixed] (b-fixed) at (2.00,-1.55)
    {Response wording (unchanged)};
  \node[condition] (b-before) at (5.70,-1.55)
    {Historical frame};
  \node[condition] (b-after) at (10.00,-1.55)
    {Fictional frame; therefore $O$, $\Gamma$, and $q^*$ also change};
  \draw[transition] (b-before.east) --
    node[transitionlabel,above] {frame changes} (b-after.west);
  \node[outcome] (b-result) at (14.55,-1.55)
    {\textbf{Evidence:} \textsc{contradicted} $\to$ \textsc{unknown}\\
     \textbf{Label:} \textcolor{casered}{\Hall{} (\textsc{contradicted})}
     $\to$ \textcolor{casegreen}{\LD}};
  \draw[transition] (b-after.east) --
    node[transitionlabel,above] {evaluate} (b-result.west);

  % Case C
  \node[caselabel] at (0,-3.10) {C};
  \node[fixed] (c-fixed) at (2.00,-3.10)
    {$O$, $\Gamma$, $\mu$, and $q^*$};
  \node[condition] (c-before) at (5.70,-3.10)
    {Observed marking $m^*$:\\asserted};
  \node[condition] (c-after) at (10.00,-3.10)
    {Observed marking $m^*$:\\hedged};
  \draw[transition] (c-before.east) --
    node[transitionlabel,above] {status marking} (c-after.west);
  \node[outcome] (c-result) at (14.55,-3.10)
    {\textbf{Evidence:} \textsc{unknown} $\to$ \textsc{unknown}\\
     \textbf{Label:} \textcolor{casered}{\Hall{} (\textsc{marking-failure})}
     $\to$ \textcolor{casegreen}{\LD}};
  \draw[transition] (c-after.east) --
    node[transitionlabel,above] {evaluate} (c-result.west);

  % Case D
  \node[caselabel] at (0,-4.65) {D};
  \node[fixed] (d-fixed) at (2.00,-4.65)
    {Fiction task context:\\$O$, $\Gamma$, and $\mu$};
  \node[condition] (d-before) at (5.70,-4.65)
    {Claim content $q^*$:\\invented establishment};
  \node[condition] (d-after) at (10.00,-4.65)
    {Claim content $q^*$:\\attribution to a real person};
  \draw[transition] (d-before.east) --
    node[transitionlabel,above] {content crosses scope} (d-after.west);
  \node[outcome] (d-result) at (14.55,-4.65)
    {\textbf{Evidence:} \textsc{unknown} $\to$ \textsc{unknown}\\
     \textbf{Label:} \textcolor{casegreen}{\LD} $\to$
     \textcolor{casered}{\Hall{} (\textsc{out-of-scope})}};
  \draw[transition] (d-after.east) --
    node[transitionlabel,above] {evaluate} (d-result.west);

  % Case E
  \node[caselabel] at (0,-6.20) {E};
  \node[fixed] (e-fixed) at (2.00,-6.20)
    {$O$, $\Gamma$, $\mu$, $q^*$, and $m^*$};
  \node[condition] (e-before) at (5.70,-6.20)
    {$\sigma$: neutral;\\surface: neutral};
  \node[condition] (e-after) at (10.00,-6.20)
    {$\sigma$: marked;\\surface: marked};
  \draw[transition] (e-before.east) --
    node[transitionlabel,above] {style only} (e-after.west);
  \node[outcome] (e-result) at (14.55,-6.20)
    {\textbf{Evidence:} \textsc{entailed} $\to$ \textsc{entailed}\\
     \textbf{Label:} \Grd{} $\to$ \Grd{}};
  \draw[transition] (e-after.east) --
    node[transitionlabel,above] {evaluate} (e-result.west);
\end{tikzpicture}%
}
\caption{\textbf{Five worked cases as a transition diagram.}
Each row keeps the fields listed as fixed in the corresponding case, shows the
before-to-after change with a directional arrow, and then shows the resulting
evidence-state and claim-label transitions. Case~B is explicitly
multifactorial; the other rows isolate a single change. The diagram renders the
constructed tests developed below and does not add an empirical comparison.}
\label{fig:case-transitions}
\end{figure}
\FloatBarrier

\paragraph{Case A: changing the task oracle}
In XSum, a summary statement may be absent from the source document while
remaining correct against external evidence \citep{maynez_faithfulness_2020}.
\textbf{Purpose.} To test that the claim label depends on which oracle the task
designates, not on the claim.
\par\textbf{Fixed.} The response span, canonical claim, and empty permission scope.
\par\textbf{Changed.} One condition uses the source document as the task oracle;
the other uses the external factuality standard.
\par\textbf{Predicted result.} Relative to the source document, the claim is
\textsc{unknown}. Because $\Gamma=\varnothing$, it receives the hallucination
label with reason code \textsc{out-of-scope}. Relative to the external task
oracle, the same claim is \textsc{entailed} and receives the grounded label.

\paragraph{Case B (multi-factor): a contextualization boundary across historical
and fictional frames}
This case is not a single-factor contrast: changing the discourse frame also
changes the task oracle, the permission scope, and the contextualized claim.
\textbf{Purpose.} To test that the recovered claim itself depends on the
discourse frame, so that identical strings need not express identical claims.
\par\textbf{Fixed.} The constructed string ``Caf\'e Verdier existed in 1920s Paris.''
\par\textbf{Changed.} The historical task uses a register that is declared
exhaustive and contains no Verdier entry. The fiction task instead uses declared
story-world constraints. Changing the frame therefore changes the task oracle
$O$, the permission scope $\Gamma$, and the contextualized claim. Because the
historical register is declared exhaustive, its silence entails the claim's
negation. A bounded but non-exhaustive oracle would instead leave the claim
\textsc{unknown}, as in Figure~\ref{fig:prompt-to-claim}.
\par\textbf{Predicted result.} In the historical task, the recovered claim
concerns the real world and receives the hallucination label with reason code
\textsc{contradicted}. In the fiction task, the recovered story-world claim is
\textsc{unknown} and receives the licensed-divergence label. This constructed
comparison illustrates why identical strings need not express identical
contextualized claims.

\paragraph{Case C: changing the observed status marking}
This controlled contrast restates the clinical pair from
Section~\ref{subsec:contract}. The permission scope allows a candidate
diagnosis. The required status marking requires an unconfirmed diagnosis to be
hedged.
\textbf{Purpose.} To test that permission alone is not enough: presentation is
part of the contract.
\par\textbf{Fixed.} The task oracle $O$, $\Gamma$, $\mu$, the proposition
that the patient has early Lyme disease, and its \textsc{unknown} evidence
state.
\par\textbf{Changed.} Only the observed status marking $m^*$.
\par\textbf{Predicted result.} ``This is early Lyme disease'' receives the
hallucination label with reason code \textsc{marking-failure}, whereas ``this may
be early Lyme disease, but serology is required'' receives the
licensed-divergence label. The proposition, the evidence state, and all three
contract fields are identical across the pair; only the wording that signals
epistemic status differs.

\paragraph{Case D: testing a scope boundary}
This case continues the fiction task of Case~B, where the declared story-world
frame already licenses invention.
\textbf{Purpose.} To test that a permission scope has a boundary, and that the
boundary is drawn around content rather than around the frame.
\par\textbf{Fixed.} The fiction task context of Case~B: its task oracle specifies
the story-world frame, its permission scope permits invention inside that frame,
and its frame-level required status marking is satisfied.
\par\textbf{Changed.} The claim concerns either the invented establishment or a
named real person presented as real history: ``Caf\'e Verdier stood on the rue
de Seine'' against ``Ernest Hemingway drank at Caf\'e Verdier.''
\par\textbf{Predicted result.} The in-frame claim about the invented
establishment can receive the licensed-divergence label. The claim about the
named real person asserts something about the actual world, falls outside
$\Gamma$, and receives the hallucination label with reason code
\textsc{out-of-scope}. The declared frame licenses invention within the story;
it does not license real-world attribution.

\paragraph{Case E: changing surface realization with a fixed claim}
This case isolates a change in stylistic realization while holding both
truth-conditional content and epistemic status fixed.
\textbf{Purpose.} To test whether claim-preserving stylistic variation leaves
the claim label unchanged.
\par\textbf{Fixed.} The task oracle $O$ of the Rosetta Stone example of
Figure~\ref{fig:claim-label-annotation}, $\Gamma$, $\mu$, the content component
$q^*$ that the decree appears in hieroglyphic, Demotic, and Greek, and the
observed status marking $m^*$.
\par\textbf{Changed.} Only requested style-marking level $\sigma$ and surface
realization; the two responses are designed as a claim-preserving style
variation.
Neutral realization: ``The decree is written in hieroglyphic, Demotic, and
Greek.''
Marked realization: ``The same message speaks three times over: once in
hieroglyphs, once in Demotic, once in Greek.''
\par\textbf{Predicted result.} Both variants recover the same canonical claim
and receive the grounded label when the task oracle entails it. The observed
style-marking level and the style-marking alignment result are reported
separately.
A difference in recovered claims or labels would mean that the evaluator
failed this claim-preserving style-variation test.
It would not, by itself, establish that style generally changes claim recovery.

Together, the cases isolate changes in the task oracle, discourse frame,
observed status marking, permission scope, and surface realization.
Their predictions define tests for the annotation and benchmark studies below.

\section{Research Agenda}\label{sec:agenda}

The priorities below define a validation sequence. Priorities~1--3 ask whether
the truth contract can be annotated, manipulated, and used to diagnose pipeline
errors. Priorities~4 and~5 address response-level evaluation and mitigation.
Priority~6 tests external validity, and Priority~7 extends the framework to
temporal updates and agentic action. Table~\ref{tab:agenda} states the open
question for each priority. The corresponding paragraphs specify study designs,
measures, and critical tests.

All seven studies should compare contract-aware evaluation with a combined
factuality and instruction-following baseline. They should measure claim-label
agreement, reason-code accuracy, and error localization. \emph{Error
localization} means identifying the pipeline stage or truth-contract field
responsible for a disputed claim label. To avoid circularity, both methods
should use independently adjudicated gold claim labels, the same claim-recovery
procedure, and the same task oracle. Researchers should also specify decision
thresholds before evaluation.

\begin{table}[!ht]
\centering
\small
\setlength{\tabcolsep}{5pt}
\renewcommand{\arraystretch}{1.10}
\caption{\textbf{The research agenda in one view.} The table lists seven
priorities and their open questions. The corresponding paragraphs give the
study designs, measures, and critical comparisons.}
\label{tab:agenda}
\begin{tabularx}{\linewidth}{@{}P{0.24\linewidth}Y@{}}
\toprule
\textbf{Priority} & \textbf{Open question}\\
\midrule
1.~Contract annotation
& Can evaluators consistently identify the task oracle $O$, permission scope
$\Gamma$, and required status marking $\mu$ from a task context?\\\addlinespace[2pt]
2.~Factorial benchmark
& Do claim labels change as predicted when one contract field changes and the rest are held fixed?\\\addlinespace[2pt]
3.~Style-robust recovery
& Does claim recovery return the same claims across matched realizations of one claim?\\\addlinespace[2pt]
4.~Response-level record
& What must be added to a claim trace to judge a complete response?\\\addlinespace[2pt]
5.~Mitigation
& Can hallucination be reduced without suppressing permitted divergence?\\\addlinespace[2pt]
6.~Generalization
& Do the contract fields remain applicable across other domains, genres, and
languages?\\\addlinespace[2pt]
7.~Temporal and agentic
& How does a contract update over time, and how does a claim-level failure
propagate into an action?\\
\bottomrule
\end{tabularx}
\end{table}
\FloatBarrier

\paragraph{1. Task-oracle and contract annotation}
The first unresolved question is whether evaluators can identify the task
oracle $O$, permission scope $\Gamma$, and required status marking $\mu$
consistently from the task context while annotating requested style-marking
level $\sigma$ separately.
Disagreement over a claim label may reflect incompatible readings of the task
oracle or contract rather than a substantive disagreement about the recovered
claim.
A public codebook should define three groups of annotations. The first covers
the unit of analysis: contextualized claims and evidence states. The second
specifies the task oracle through its type, admissible sources, temporal scope,
and authority ordering, together with permission scope and required status
marking. The third covers requested and observed style-marking levels and
claim-preserving style variation.
Pilot studies should annotate each field separately, preserve genuine
disagreement, and report agreement by field and label, adjudication rate,
annotation time, and sources of disagreement.
Contract-inference studies should document how user requests, system
instructions, domain constraints, and safety or legal rules are reconciled,
and how clarifications update the contract in multi-turn interaction.
A single task context fixes $O$, $\Gamma$, and $\mu$, so annotators can reuse
these field annotations across responses generated in that context. Claim
recovery, canonicalization, and verification must still be repeated for each
response.
\emph{Critical test.} Persistent disagreement, or an annotation cost out of
proportion to the diagnostic gain, would count against the position.

\paragraph{2. Factorial benchmark design}
Naturally occurring evaluation data often change the task oracle, permission
scope, required status marking, and surface realization together, preventing
attribution of a claim-label change to one field.
Without controlled contrasts, the proposed claim-labeling rule cannot be
distinguished from a combined factuality and instruction-following baseline.
A controlled factorial benchmark should therefore vary the task oracle
$O$, permission scope $\Gamma$, or required status marking $\mu$ while
holding the remaining fields and target claim fixed.
Separate claim-preserving style-variation pairs should realize matched
canonical claims at $\sigma\in\{0,1,2\}$ while keeping $O$, $\Gamma$, and
$\mu$ fixed.
These contrasts test a hypothesis about claim recovery; they do not assume
that style changes the recovered claim set.
Evaluation should report claim-set equivalence, evidence state, claim label,
accuracy of observed style-marking level annotation, prompt--response
style-marking alignment, and performance of the combined factuality and
instruction-following baseline.
\emph{Critical test.} Failure to preserve factual commitment across controlled
variants, or a combined baseline that matches contract-aware evaluation, would
count against the position.

\paragraph{3. Style-robust claim recovery and error localization}
An incorrect label can enter at several stages: span selection, claim recovery,
canonicalization, observed style-marking annotation, verification against the
task oracle, or application of $\Gamma$ and $\mu$. An end-to-end hallucination
score cannot show which stage failed. An apparent method advantage may
therefore reflect an upstream claim-recovery difference rather than a better
labeling rule.
Claim recovery and verification pipelines should be tested on matched variants
at requested style-marking levels $0$, $1$, and $2$ containing metaphor,
irony, analogy, narrative voice, hypotheses, or declared fiction.
Style-marking level remains outside the truth contract in every condition.
For each response, the evaluation trace should record the recovered canonical
claims, evidence states, scope and marking decisions, claim labels, reason
codes, observed style-marking level, and prompt--response style-marking
alignment. For each requested style-marking level, comparisons among
claim-extraction, natural-language-inference, retrieval, and model-judge
pipelines should report accuracy and calibration separately for each pipeline
stage.
Measures should include omission, spurious-claim creation,
under-decomposition, over-decomposition, and resulting inflation or deflation of
\Grd{}, \Hall{}, and \LD{} counts.
The same claim recovery procedure should also be tested with and without
access to the requested style-marking level while the response and the three
contract fields remain fixed.
\emph{Critical test.} A lack of consequential diagnostic gain over end-to-end
evaluation would count against the position.

\paragraph{4. Response-level record, global reasoning, and aggregation}
Claim-level labels do not establish whether a complete response reasons
validly, remains coherent, covers the required information, or answers the
question.
A response may contain only grounded and licensed-divergence claims yet
combine them through an invalid inference, contradict itself, omit a necessary
step, or remain irrelevant to the task.
Studies should evaluate three groups of properties separately: claim labels,
task-oracle adequacy, and claim-recovery coverage; global logical validity,
cross-claim coherence, completeness, and relevance; and style-marking alignment
and creative-task success. They should then compare explicit rules for
assigning the content verdicts \textsc{contract-compliant},
\textsc{contract-violating}, and \textsc{indeterminate}, together with any
optional aggregation used for ranking.
Decision rules, severity policies, and task-specific thresholds should be
declared before evaluation, and sensitivity analyses should report whether
model rankings or substantive conclusions change across plausible rules.
\emph{Critical test.} If the claim trace alone predicts independently
adjudicated response-level correctness and creative-task success, the additional
response-level components would add no diagnostic value. That result would
count against the position.

\paragraph{5. Mitigation without indiscriminate suppression}
A mitigation method can lower a conventional hallucination rate by increasing
abstention, avoiding figurative language, or suppressing claims with evidence
state \textsc{unknown} indiscriminately.
Such a reduction would not show whether reliability improved or the model
merely became more conservative.
Retrieval, verification, abstention, and constrained decoding should be
evaluated on five distinct outcomes: reduction of \Hall-labeled claims,
preservation of \LD-labeled claims, preservation of \Grd-labeled claims,
style-marking alignment, and creative-task success when applicable.
The first three are claim-label outcomes; the last two are separate
response-level dimensions.
Because creative-text judgments vary across readers, creative-task scores
should report uncertainty
\citep{diedrich_are_2015,marco_reader_2025}.
Results should be reported separately by contract, or through Pareto
comparisons, so that trade-offs among these outcomes remain visible.
\emph{Critical test.} If a method improves only by suppressing permitted
divergence, that result would count against the position.

\paragraph{6. Generalization across domains and languages}
The contract fields and three-level style-marking scale may work in the
constructed examples yet fail when task-oracle types, adjudication standards,
hedging conventions, genres, or instruction hierarchies change.
The annotation and contrast protocols should therefore be tested in
high-stakes factual settings, such as clinical decision support, as well as in
education, journalism, scientific communication, brainstorming, and fiction.
Studies should report field-level agreement separately by domain. This
reporting would show which contract field accounts for any decline rather than
attributing it to the domain as a whole.
Multilingual studies should test whether style-marking conventions, epistemic
hedges, and genre conventions support comparable annotations across languages
and cultures. Required status marking is the most exposed field because
languages grammaticalize epistemic commitment differently. Studies should
therefore report agreement on $\mu$ separately for each language.
\emph{Critical test.} Systematic domain- or language-specific disagreement
would limit the claimed generality of the framework and count against the
position.

\paragraph{7. Temporal truth-contract updates and agentic action}
In multi-step interaction, the task oracle and applicable instructions may
change over time, while later actions may consume earlier claims.
A static truth contract cannot represent an update to $O_t$ or show how a
\textsc{marking-failure} propagates from a claim into an action.
Agentic studies should model a time-indexed task oracle $O_t$ that can
incorporate tool results, environment state, updated retrieval, and human
approval.
They should assign each subtask its own truth contract and trace claim labels
and reason codes into downstream actions.
Measures should include task-oracle update accuracy, cross-subtask error
propagation, and whether an action relies on a claim that failed required
status marking.
\emph{Critical test.} If static evaluation performs equally well on the
action-level criteria, time-indexed contracts would add no diagnostic value.
That result would count against the position.


Together, these studies test the framework from annotation reliability to
downstream action. Until such evidence is available, the implications of the
position must remain limited to the distinctions stated below.

\section{Discussion}\label{sec:discussion}

\subsection{What Is Relative and What Is Not}\label{subsec:relative}

The term \emph{relative} has a restricted evaluative meaning in this paper.
The truth of a proposition is not task-relative, but its classification as a
contract violation can be.
The task oracle determines whether the proposition is entailed, contradicted,
or unknown; permission scope and required status marking determine how an
unknown claim is labeled.
The truth contract therefore changes the evaluation label, not what is true.
A licensed-divergence label records permission and compliant presentation; it
does not establish the truth of the claim.

This distinction also limits what follows from theoretical lower bounds.
\citet{kalai_calibrated_2024} derive a statistical lower bound for
hallucination on certain arbitrary facts under a generative-model calibration
condition.
\citet{xu_hallucination_2024} prove an impossibility result for computable
models used as general problem solvers over computable ground-truth functions,
and \citet{banerjee_llms_2024} argue for structural non-zero hallucination risk
from computability and undecidability limits.
These results rely on different assumptions; none alone implies a constant
deployment error rate or precludes statistically negligible rates in a
specified setting \citep{suzuki_negligible_2025}.

These lower bounds concern what is unavoidable in principle; they do not
describe current deployment error rates. Scale and recency are also not
sufficient proxies for those rates. AA-Omniscience tests closed-book factual
reliability by asking models to answer when confident and abstain otherwise. Its
Omniscience Index subtracts incorrect from correct answers across all items. It
also reports a benchmark-specific hallucination rate: the share of non-correct
answers that are confidently wrong rather than partial or declined
\citep{jackson_aaomniscience_2025}.
Its launch analysis found that contemporaneous overall capability did not
reliably predict a low hallucination rate and that larger open-weight models did
not consistently hallucinate less. An August 2026 leaderboard snapshot likewise
shows wide dispersion across disclosed parameter counts and release dates
(Appendix~\ref{app:omniscience}). We draw only this conclusion: scale, general
capability, and recency are not sufficient proxies for factual reliability.
The question of how an evaluation should treat evidence-unknown content
therefore does not disappear as models improve.

The theoretical lower bounds also do not imply that every claim left unknown by
the task oracle should be suppressed.
Such suppression would also remove permitted hypotheses and in-frame
invention.
Empirical and conceptual work reports possible trade-offs between hallucination
mitigation and creativity
\citep{banerjee_does_2025,sun_hallucinating_2024,he_shakespearean_2025}.
One study further reports that preference-alignment strategies that reduce
hallucination can also lower the output uncertainty associated with literary
quality \citep{sui_uncertainty_2026}.

\subsection{Response-Level Interpretation}

Appendix~\ref{app:diagnostics} defines the ordered content-verdict rule and the
two additional components of the response-level record: style-marking alignment
and creative-task success. This subsection states what that record does
\emph{not} establish, because readers might otherwise overinterpret it.

\textsc{Contract-compliant} is a weaker guarantee than it sounds. It does not
mean the response is true. A response containing only licensed-divergence
claims is contract-compliant even if every claim is false. In that case, the
licensed-divergence labels record that the task authorized the divergence and
that the response marked it as required. They do not record truth. A compliance
rate would therefore be misleading if read as a reliability score in the tasks
for which the framework was designed.

\textsc{Indeterminate} is an evaluation outcome, not a missing value. It means
that the evaluation could not be completed reliably. The failure lies in the
evaluation procedure, not necessarily in the response. A benchmark that
discards indeterminate items or folds them into another verdict converts a
measurement failure into a claim about the model. Claim-recovery coverage
requires an independently adjudicated set of commitments. The rate of
indeterminate verdicts therefore also reflects how well the benchmark was
constructed and should be reported as such.

Severity sits outside the rule for a different reason than the two reported
components: it is not a competing dimension but a weight applied afterwards.
An invented drug dose in the clinical task and an incorrect date on the museum
label both receive the hallucination label although their consequences differ.
Severity may order violations for triage or mitigation.
It must not decide whether a violation occurred.
If it did, the label would depend on the evaluator's estimate of harm rather
than on the contract, and two evaluators with different risk models would
disagree about a matter of fact.

The response-level record should therefore accompany the claim trace. Any
scalar derived from it answers a further, task-dependent question. Comparisons
based on such a score should report their sensitivity to the chosen aggregation
policy. The evaluation target remains text that is grounded where the task
requires grounding, transparent where divergence is permitted, and aligned
with the requested style-marking level.

\subsection{Limitations and Safeguards}

The proposal has seven immediate limitations.
First, claim-level labels do not establish response-level validity, coherence,
completeness, or relevance. A favorable distribution of claim labels therefore
supports no conclusion about the response as a whole. Each property requires a
separate test (Priority~4), as Section~\ref{subsec:global} explains.

Second, an ambiguous or inconsistent task context may prevent stable
truth-contract specification.
Priority~1 turns this limitation into an annotation question. The evaluator
must state how system and user instructions are prioritized and which safety,
legal, and domain constraints apply. Otherwise, it records a truth-contract
specification failure and leaves the affected claim unlabeled.

Third, the task oracle is idealized as the adjudication standard that should be
applied, but a concrete implementation may be incomplete, outdated,
inaccessible, or internally conflicting.
The evaluator should record a task-oracle adequacy or adjudication failure
rather than automatically assigning the \textsc{unknown} evidence state
(Priority~1).

Fourth, span selection, claim recovery, and canonicalization require judgment.
Validation therefore requires independently adjudicated canonical claims and
separate reporting of procedural failures and claim-recovery coverage
(Priority~3).

Fifth, permission scope must be specified from the task context before claim
labeling and cannot be widened after inspecting a response to turn a
fabrication into licensed divergence.
Applicable safety, legal, and domain constraints also limit any permission scope
specified by the user.

Sixth, the three-level style-marking scale is not a validated universal model
of style. Whether style affects claim recovery remains a testable hypothesis
(Priorities~2 and~3). Style-marking alignment alone is not a measure of
creativity.

Seventh, the purposive mapping reports our own coding of the selected
resources and is scoped to them by design.
Future work could independently recode these resources or estimate the
prevalence of the identified pattern in the broader literature. The published
resource-level coding in Appendix~\ref{app:mapping} makes both extensions
possible.

The framework is a claim-centered evaluation procedure with a structured
response-level interpretation; it is not a universal measure of truth,
reasoning, creativity, usefulness, or safety.


\section{Conclusion}\label{sec:conclusion}

We have argued that hallucination is relative to a task's truth contract.
This relativity concerns the evaluation label, not the truth value of the
claim: the contract changes the label, not what is true.

The truth contract makes explicit the task oracle, permission scope, and
required status marking.
An entailed claim receives the grounded label and a contradicted claim
receives the hallucination label.
An unknown claim receives the licensed-divergence label only when the task
permits it and the response marks it as required; otherwise it receives the
hallucination label with a reason code.

The framework labels claims individually. At response level, it reports a
three-component record: a content verdict, style-marking alignment, and
creative-task success. The content verdict is derived from the claim trace
together with claim-recovery coverage and task-oracle adequacy; a blocking
procedural failure makes that verdict \textsc{indeterminate}. Global reasoning,
completeness, and relevance require separate evaluation. Style-marking alignment
and creative-task success never change a claim label. Usefulness and severity
are likewise separate diagnostics: they may weight a violation after labeling,
but they do not determine whether one occurred.

In our selected sample, no resource separately represents the task oracle,
permission scope, and required status marking while also controlling
claim-preserving style variation.
The immediate empirical question is whether contract-aware annotation adds
information beyond a combined factuality and instruction-following baseline.
The comparison should measure claim-label agreement, reason-code accuracy, and
error localization, together with a predeclared measure of response-level
interpretation. If the baseline matches contract-aware annotation on every
measure, the truth contract adds explicitness without adding discriminative
power. That result would count against the position.
The objective is not to suppress all divergence, but to produce responses that
are reliable where truth is required and transparently marked where divergence
is permitted, with creative success assessed under a task-specific criterion.

\clearpage

\appendix

\appendixsection{Response-Level Record, Procedural Failures, and Optional Aggregation}%
\phantomsection
\makeatletter\def\@currentlabel{A}\makeatother\label{app:diagnostics}

Section~\ref{sec:framework} defines the truth contract, claim recovery,
canonicalization, claim verification, the claim-labeling rule, and the
style-marking alignment check.
Those definitions govern decisions about individual claims. This appendix
specifies what an evaluation publishes after those decisions. It defines the
response-level record, the procedural failures that can prevent labeling, the
stability test for claim recovery under claim-preserving style variation, and
the role of optional aggregation.

\paragraph{From claim labels to a response-level record}

Claim labels are the primary output of the framework, but users and benchmark
designers also need a conclusion about the complete response.
The evaluation should therefore publish the claim-level labels and reason
codes, claim-recovery coverage, and any procedural failures together with the
response-level record.
\emph{Claim-recovery coverage} is the proportion of the response's
truth-conditional commitments that claim recovery actually recovered and
canonicalized.
Estimating this proportion requires an independently adjudicated set of
attributable commitments.

The response-level record has three components. The first is a \emph{content
verdict}.
The default rule applies three tests in order.
\begin{enumerate}
  \item If any reliably adjudicated claim receives \Hall, the response is
        \textsc{contract-violating}.
  \item Otherwise, the response is \textsc{indeterminate} if claim-recovery
        coverage falls below a predeclared threshold or if a blocking
        truth-contract-specification, task-oracle, or adjudication failure
        prevents a reliable verdict.
  \item Otherwise, every adjudicated claim received \Grd{} or \LD, and the
        response is \textsc{contract-compliant}.
\end{enumerate}
A benchmark may use a different predeclared aggregation policy, but it must
declare that policy before evaluation and retain the underlying claim trace.
Aggregation policies may include the default worst-case rule, a proportional
rule based on the distribution of claim labels, severity-weighted aggregation,
or task-specific thresholds.
No one policy is assumed to be universally appropriate.
Licensed divergence counts as contract-compliant because it satisfies the
permission and marking conditions; it does not count as established truth.

The second component is \emph{style-marking alignment}: \textsc{match},
\textsc{mismatch}, or \textsc{not applicable}, as defined above.
The third is \emph{creative-task success}, which is reported separately as
\textsc{pass}, \textsc{fail}, \textsc{not assessed}, or a task-specific score
under an explicit rubric for properties such as novelty, usefulness, quality,
or diversity.
A style match is not sufficient evidence of creativity, and creative success
cannot convert a hallucination-labeled claim into licensed divergence.
The target is a response that is grounded where the task requires grounding,
transparently marked where divergence is permitted, and stylistically aligned
with the requested form.

The response-level record is not a universal scalar. Severity, usefulness,
global logical validity, cross-claim coherence, completeness, and relevance may
be reported as task-specific diagnostics. They must remain separate and cannot
retroactively change claim labels.
In this paper, \emph{usefulness} is the task-relative value of a permitted
claim or response for the stated user goal; its unit of analysis must be
reported explicitly.
Existing factuality evaluation reports both claim-aggregated scores and
benchmark-level decisions \citep{min_factscore_2023,bayat_factbench_2024}.
Any scalar aggregation answers an additional, task-dependent question. It
should be reported together with the response-level record and the claim trace.

\paragraph{Procedural failures}

A \emph{procedural failure} is a breakdown in the evaluation procedure itself
rather than in the response.
It is recorded in place of a claim label, and the affected span is left
unlabeled; it never receives \Grd, \Hall, or \LD.
Three conditions must hold before any claim label can be assigned: the
evaluator must obtain a stable canonical claim, the task context must determine
the three contract fields, and claim verification must resolve the claim to one
evidence state.
Each condition can fail in a distinct way.

\paragraph{Failures of claim recovery}
The evaluator may be unable to obtain a stable canonical claim.
Span-selection, claim recovery, canonicalization, and matching errors all have
this effect.
They may cause an evaluator to omit a recoverable claim, create a spurious
canonical claim, under-decompose distinct commitments into one claim,
over-decompose one commitment into several claims, or alter $q^*$ or $m^*$.
If no stable canonical claim can be obtained, no claim label is assigned to the
affected span.

\paragraph{Failures of the truth contract or task oracle}
The evaluator may instead be unable to apply the contract to a claim it has
recovered. Four failures have this effect.
\begin{description}
  \item[Truth-contract specification failure.] The task context does not
        determine $O$, $\Gamma$, and $\mu$.
  \item[Task-oracle specification failure.] $O$ does not identify admissible
        sources, temporal scope, authority relations, or a conflict-resolution
        procedure precisely enough for the task.
  \item[Task-oracle adequacy failure.] The specified oracle is incomplete,
        outdated, inaccessible, internally conflicting, or insufficient for the
        claim being evaluated.
  \item[Adjudication failure.] The evaluator cannot resolve the task oracle
        into a single evidence state.
\end{description}
In each case the evaluator records the failure and leaves the affected span
unlabeled.
Silence is not one of these failures: a correctly instantiated bounded task
oracle that neither entails nor contradicts a claim yields the
\textsc{unknown} evidence state, which the contract is designed to handle.
A blocking procedural failure produces an \textsc{indeterminate} content
verdict rather than an automatic \textsc{unknown} evidence state.
Recent fact-verification work shows that these outcomes are easy to conflate in
practice. How an \textsc{unknown} evidence condition is constructed shapes what
a verifier learns and can hide genuine adjudication difficulty
\citep{qiu_evidence_2026}. Separately, a single global faithfulness label can
mask sentence-level adjudication disagreement through an averaging bias
\citep{zhang_averaging_2026}.

\paragraph{An example}
Suppose two annotators read the closing span of a museum panel, ``the winter
that would not lift''.
One recovers a claim about the severity of that winter; the other recovers no
truth-conditional claim at all.
The two annotators disagree about which claim the span commits to, not about
which label a shared claim deserves.
The evaluation therefore records a claim-recovery failure for that span instead
of a label disagreement.

A response that does not realize the requested style-marking level is not a
procedural failure.
That mismatch is recorded through style-marking alignment, whereas a canonical
claim that violates the truth contract is handled by the claim-labeling rule.


\paragraph{Claim-preserving style variation test}
Consider three expert-adjudicated responses $y_0,y_1,y_2$ produced under task
contexts that differ only in requested style-marking level, with
$\sigma_{p_0}=0$, $\sigma_{p_1}=1$, and $\sigma_{p_2}=2$.
The test asks two questions: do the responses yield the same canonical claims,
and do those matched claims receive the same labels? After one-to-one
adjudicated matching, let $\mathcal C^*(y_i,p_i)$ denote each set of canonical
claims $c^*=(q^*,m^*)$. Claim-preserving style variation requires the
matched-claim-set condition
$\mathcal C^*(y_0,p_0)=\mathcal C^*(y_1,p_1)=\mathcal C^*(y_2,p_2)$.
Thus, matched claims retain the same $q^*$ and $m^*$ components, although their
surface strings and observed style-marking levels may differ. With $O$,
$\Gamma$, and $\mu$ fixed, every matched canonical claim $c_j^*$ must also
receive the same claim label (\Grd, \Hall, or \LD) under $y_0,p_0$, $y_1,p_1$,
and $y_2,p_2$.

\begin{itemize}
  \item \emph{Claim recovery error}: a recoverable claim is dropped or a
        spurious claim is added during claim recovery.
  \item \emph{Canonicalization error}: canonicalization alters $q^*$ or
        $m^*$.
  \item \emph{Claim-label instability}: matched canonical claims receive
        different labels when $O$, $\Gamma$, and $\mu$ are fixed.
  \item \emph{Style-marking alignment error}: the evaluator
        incorrectly classifies the match between requested and observed
        style-marking levels.
  \item \emph{Optional-aggregation sensitivity}: a response comparison
        changes when the evaluator changes an optional rule applied to the
        claim-level labels and diagnostics.
\end{itemize}

Claim recovery and claim-label diagnostics require expert-adjudicated canonical
claim matches.
Style-marking alignment errors require an independently
adjudicated comparison between requested and observed style-marking levels.
Claim-label instability is an error only when $O$, $\Gamma$, and $\mu$ remain fixed.
When $\Gamma$ or $\mu$ changes, the same \textsc{unknown} claim may
intentionally change from the hallucination label to the licensed-divergence
label; the evidence state remains \textsc{unknown}.

Optional scalar aggregation may support ranking, but it does not replace the
response-level record or its component diagnostics. Comparisons should report
sensitivity to plausible aggregation rules rather than present one rule as
uniquely determined.

\appendixsection{Resource Mapping}%
\phantomsection
\makeatletter\def\@currentlabel{B}\makeatother\label{app:mapping}
\renewcommand*{\theHtable}{B.\arabic{table}}

Tables~\ref{tab:full-mapping-factual-a}--\ref{tab:full-mapping-creative} list the forty-one resources used in the purposive mapping.
Each row reports a task-oracle type and its task-specific instantiation, a
separate quality or task-success criterion, treatment of requested
style-marking level, permission scope $\Gamma$, and required status marking
$\mu$ under the nominal task profiles used in Figure~\ref{fig:profiles}.

\paragraph{Coding vocabulary}
Each coded field uses one value from the following list. Parenthetical text may
retain a task-specific qualifier, but it does not create an additional code.

\begin{description}
  \item[Nominal task profile.]
  \emph{strict grounding};
  \emph{intent evaluation};
  \emph{declared-frame invention and ideation};
  \emph{constrained solution search};
  \emph{heterogeneous suites and tool use}.

  \item[Task oracle $O$.]
  \emph{source document}, a supplied document treated as authoritative for
  claim adjudication;
  \emph{retrieval set}, retrieved items used to verify claims;
  \emph{database or knowledge base}, a structured factual resource;
  \emph{gold labels}, reference answers or labels used as the adjudication
  standard;
  \emph{environment or tool state}, observable state or tool output;
  \emph{human adjudication}, a specified human decision procedure;
  \emph{fictional-world constraints}, declared canon or story-world
  constraints;
  \emph{mixed or task-dependent}, when the type varies across subtasks or
  combines types;
  \emph{none identifiable}, when our coding finds no standard that can
  establish or contradict the evaluated claims.
  This field records the claim-adjudication standard, not a general quality or
  task-success criterion.

  \item[Quality or task-success criterion.]
  \emph{not separately represented};
  \emph{quality or preference rubric};
  \emph{novelty or divergent-thinking rubric};
  \emph{feasibility or correctness criterion};
  \emph{specification or tests};
  \emph{mixed}.
  Here, \emph{not separately represented} means that no additional criterion
  is reported beyond agreement with the task oracle; ordinary accuracy against
  $O$ is not recoded as a separate task-success dimension.

  \item[Requested style-marking treatment.]
  \emph{not controlled};
  \emph{not controlled or not applicable};
  \emph{scored, not claim-controlled}.

  \item[Permission scope $\Gamma$.]
  \emph{inferred empty};
  \emph{implicit frame};
  \emph{partial};
  \emph{task-specific};
  \emph{subtask-specific};
  \emph{mixed};
  \emph{not separately represented}.
  A specification, test, or query constraint is not coded as permission scope
  unless the protocol uses it to authorize evidence-unknown content.

  \item[Required status marking $\mu$.]
  \emph{not scored} means that required status marking is not evaluated as a
  separate dimension.
  \emph{not separated} means that a relevant required-status-marking criterion
  is absorbed into a broader task or rubric rather than coded independently.
\end{description}

The original resources do not necessarily use the term \emph{task oracle}.
These codes record the authors' interpretation of the adjudication standard
available in each evaluation design; they do not imply that a source uses
truth-contract terminology.
A preference score, quality rubric, novelty score, general task-success
signal, LLM-judge preference, or nonfactual user instruction is not by itself
a task oracle.
It remains in the quality or task-success column unless the evaluation uses it
to establish or contradict a claim.

\begin{table*}[t]
\centering
\scriptsize
\setlength{\tabcolsep}{3pt}
\renewcommand{\arraystretch}{1.0}
\caption{\textbf{Factuality and faithfulness resources in the selected
purposive mapping (part I).}
Task-oracle type is coded separately from quality or task success.
``Not controlled'' does not mean that a neutral style-marking level is fixed.}
\label{tab:full-mapping-factual-a}
\begin{tabularx}{0.99\linewidth}{
  @{}P{0.17\textwidth}P{0.25\textwidth}
  P{0.15\textwidth}P{0.14\textwidth}Y@{}}
\toprule
\textbf{Resource}
& \textbf{Task and task oracle $O$}
& \textbf{Quality / success criterion}
& \textbf{Requested style-marking treatment}
& \textbf{$\Gamma$ permission scope /\newline $\mu$ status marking}\\
\midrule
\multicolumn{5}{l}{\textbf{Nominal profile: strict grounding}}\\
1.~FEVER \citep{thorne_fever_2018} & Fact checking\newline $O$: source document (Wikipedia evidence sentences) & not separately represented & not controlled & $\Gamma$: inferred empty\newline $\mu$: not scored\\
2.~TruthfulQA \citep{lin_truthfulqa_2022} & Factual QA\newline $O$: mixed or task-dependent (sourced reference answers; human adjudication for generations) & quality or preference rubric (informativeness) & not controlled & $\Gamma$: inferred empty\newline $\mu$: not scored\\
3.~PopQA \citep{mallen_when_2023} & Long-tail QA\newline $O$: database or knowledge base (Wikidata) & not separately represented & not controlled & $\Gamma$: inferred empty\newline $\mu$: not scored\\
4.~SimpleQA \citep{wei_measuring_2024} & Factual QA\newline $O$: gold labels (verified reference answers) & not separately represented & not controlled & $\Gamma$: inferred empty\newline $\mu$: not scored\\
5.~TriviaQA \citep{joshi_triviaqa_2017} & Reading QA\newline $O$: mixed or task-dependent (gold answers plus Web/Wikipedia documents) & not separately represented & not controlled & $\Gamma$: inferred empty\newline $\mu$: not scored\\
6.~DefAn \citep{rahman_defan_2024} & Factual QA\newline $O$: gold labels (definitive reference answers) & mixed (prompt alignment and cross-run consistency) & not controlled & $\Gamma$: inferred empty\newline $\mu$: not scored\\
7.~HaluEval \citep{li_halueval_2023} & Multi-task hallucination recognition\newline $O$: mixed or task-dependent (task references or source text; Web-assisted adjudication) & mixed (specificity and relevance folded into some labels) & not controlled & $\Gamma$: inferred empty\newline $\mu$: not scored\\
8.~FactBench \citep{bayat_factbench_2024} & Open-ended factuality evaluation\newline $O$: retrieval set (Web-retrieved material) & not separately represented & not controlled & $\Gamma$: inferred empty\newline $\mu$: not scored\\
9.~HalluLens \citep{bang_hallulens_2025} & Extrinsic/intrinsic hallucination benchmark\newline $O$: mixed or task-dependent (Wikipedia, nonexistent entities, or task context) & mixed (correctness and refusal/acceptance) & not controlled & $\Gamma$: inferred empty\newline $\mu$: not separated\\
10.~FRANK \citep{pagnoni_understanding_2021} & Summarization\newline $O$: source document (source documents) & not separately represented & not controlled & $\Gamma$: inferred empty\newline $\mu$: not scored\\
11.~QAGS \citep{wang_asking_2020} & Summarization\newline $O$: source document (source documents) & not separately represented & not controlled & $\Gamma$: inferred empty\newline $\mu$: not scored\\
12.~SummEval \citep{fabbri_summeval_2021} & Summarization\newline $O$: source document (source documents) & mixed (automatic metrics and human summarization-quality judgments) & not controlled & $\Gamma$: inferred empty\newline $\mu$: not scored\\
13.~XSum \citep{narayan_xsum_2018} & Summarization\newline $O$: source document (BBC source article) & mixed (ROUGE, human preference, and information preservation) & not controlled & $\Gamma$: inferred empty\newline $\mu$: not scored\\
\bottomrule
\end{tabularx}
\end{table*}

\clearpage
\begin{table*}[t]
\centering
\scriptsize
\setlength{\tabcolsep}{3pt}
\renewcommand{\arraystretch}{1.0}
\caption{\textbf{Factuality, faithfulness, and adjacent resources in the
selected purposive mapping (part II).}
Rows 14--22 continue strict grounding, row 23 represents intent evaluation,
and rows 24--27 cover heterogeneous or task-dependent conditions.}
\label{tab:full-mapping-factual-b}
\begin{tabularx}{0.99\linewidth}{
  @{}P{0.17\textwidth}P{0.25\textwidth}
  P{0.15\textwidth}P{0.14\textwidth}Y@{}}
\toprule
\textbf{Resource}
& \textbf{Task and task oracle $O$}
& \textbf{Quality / success criterion}
& \textbf{Requested style-marking treatment}
& \textbf{$\Gamma$ permission scope /\newline $\mu$ status marking}\\
\midrule
\multicolumn{5}{l}{\textbf{Nominal profile: strict grounding (continued)}}\\
14.~CNN/DailyMail \citep{hermann_teaching_2015,wang_asking_2020} & News corpus used in summarization evaluation\newline $O$: source document (news source article) & not separately represented & not controlled & $\Gamma$: inferred empty\newline $\mu$: not scored\\
15.~DelucionQA \citep{sadat_delucionqa_2023} & Domain QA\newline $O$: source document (retrieved chunks from one Jeep Gladiator manual) & mixed (answerability, coverage, and abstention) & not controlled & $\Gamma$: inferred empty\newline $\mu$: not separated\\
16.~HalluQA \citep{cheng_evaluating_2023} & Chinese adversarial QA\newline $O$: gold labels (answer sets backed by sources or explanations) & mixed (fluency and directness folded into the verdict) & not controlled & $\Gamma$: inferred empty\newline $\mu$: not separated\\
17.~CR-LLM \citep{li_cr-llm_2024} & Concept reasoning\newline $O$: mixed or task-dependent (reference concepts plus GPT-4 contextual adjudication) & quality or preference rubric (concept granularity) & not controlled & $\Gamma$: inferred empty\newline $\mu$: not scored\\
18.~HotpotQA \citep{yang_hotpotqa_2018} & Multi-hop QA\newline $O$: source document (Wikipedia evidence passages) & not separately represented & not controlled & $\Gamma$: inferred empty\newline $\mu$: not scored\\
19.~HalluRAG \citep{ridder_hallurag_2025} & RAG hallucination detection\newline $O$: mixed or task-dependent (provided passages/answers and derived sentence labels) & not separately represented & not controlled & $\Gamma$: inferred empty\newline $\mu$: not separated\\
20.~RealTime QA \citep{kasai_realtime_2024} & Real-time QA\newline $O$: gold labels (time-indexed reference answers) & not separately represented & not controlled & $\Gamma$: inferred empty\newline $\mu$: not scored\\
21.~HypoTermQA \citep{uluoglakci_hypotermqa_2024} & Hypothetical-term evaluation\newline $O$: mixed or task-dependent (term-validity labels and Wikipedia definitions) & not separately represented & not controlled & $\Gamma$: inferred empty\newline $\mu$: not separated\\
22.~AA-Omniscience \citep{jackson_aaomniscience_2025,artificialanalysis_aaomniscience_public_2025} & Cross-domain factual QA\newline $O$: gold labels (source-derived reference answers, evaluated by an LLM grader) & mixed (accuracy, incorrect-answer penalty, and abstention-sensitive reliability) & not controlled & $\Gamma$: inferred empty\newline $\mu$: not separated\\
\midrule
\multicolumn{5}{l}{\textbf{Nominal profile: intent evaluation}}\\
23.~FaithQA \citep{hao_faithqa_2025} & Intent verification\newline $O$: mixed or task-dependent (RAG reports; Wikipedia for factual QA; none for creative writing) & specification or tests (query-constraint satisfaction) & not controlled & $\Gamma$: not separately represented\newline $\mu$: not scored\\
\midrule
\multicolumn{5}{l}{\textbf{Nominal profile: heterogeneous suites and tool use}}\\
24.~BIG-Bench \citep{srivastava_beyond_2023} & Multi-task reasoning\newline $O$: mixed or task-dependent (subtask targets and adjudication) & not separately represented & not controlled or not applicable & $\Gamma$: not separately represented\newline $\mu$: not scored\\
25.~BIG-Bench Hard \citep{suzgun_challenging_2023} & Reasoning suite\newline $O$: gold labels (task-specific reference answers) & not separately represented & not controlled or not applicable & $\Gamma$: not separately represented\newline $\mu$: not scored\\
26.~ToolQA \citep{zhuang_toolqa_2023} & Tool-use QA\newline $O$: mixed or task-dependent (fixed corpora and generated gold answers) & not separately represented & not controlled or not applicable & $\Gamma$: not separately represented\newline $\mu$: not scored\\
27.~HIC-Bench \citep{yang_hicbench_2025} & Hallucination typing\newline $O$: mixed or task-dependent (CDKB/Wikipedia principles and human-verified judgments) & mixed (originality, feasibility, value, and composite metrics) & not controlled & $\Gamma$: not separately represented\newline $\mu$: not scored\\
\bottomrule
\end{tabularx}
\end{table*}

\clearpage
\begin{table*}[t]
\centering
\scriptsize
\setlength{\tabcolsep}{3pt}
\renewcommand{\arraystretch}{0.85}
\caption{\textbf{Creativity and constrained-problem-solving resources in the
selected purposive mapping.}
Task oracle and task-success criteria are reported separately; a quality or
creativity score is not treated as an oracle unless it can establish or
contradict an evaluated claim.}
\label{tab:full-mapping-creative}
\begin{tabularx}{0.99\linewidth}{
  @{}P{0.17\textwidth}P{0.25\textwidth}
  P{0.15\textwidth}P{0.14\textwidth}Y@{}}
\toprule
\textbf{Resource}
& \textbf{Task and task oracle $O$}
& \textbf{Quality / success criterion}
& \textbf{Requested style-marking treatment}
& \textbf{$\Gamma$ permission scope /\newline $\mu$ status marking}\\
\midrule
\multicolumn{5}{l}{\textbf{Nominal profile: declared-frame invention and ideation}}\\
28.~CreativityPrism \citep{hou_creativityprism_2025} & Multi-task creativity\newline $O$: mixed or task-dependent (tests or correctness judges where applicable) & mixed (task-specific rubrics) & not controlled & $\Gamma$: implicit frame (task)\newline $\mu$: not separated\\
29.~LitBench \citep{fein_litbench_2025} & Story evaluation\newline $O$: none identifiable (human preferences assess quality) & quality or preference rubric (human preferences) & not controlled & $\Gamma$: implicit frame (story)\newline $\mu$: not separated\\
30.~Short Story \citep{ismayilzada_evaluating_2025} & Story generation\newline $O$: none identifiable (cue words constrain the task, not claim truth) & mixed (novelty, diversity, complexity, and judged creativity) & not controlled & $\Gamma$: implicit frame (story)\newline $\mu$: not separated\\
31.~WritingBench \citep{wu_writingbench_2025} & Generative writing\newline $O$: mixed or task-dependent (provided materials when present) & quality or preference rubric (writing rubric) & scored, not claim-controlled & $\Gamma$: implicit frame (task)\newline $\mu$: not separated\\
32.~Art-or-Artifice / TTCW \citep{chakrabarty_art_2024} & Creative writing\newline $O$: none identifiable (expert tests assess creativity) & quality or preference rubric (expert rubric) & not controlled & $\Gamma$: implicit frame (story)\newline $\mu$: not separated\\
33.~Automated DT scoring \citep{organisciak_beyond_2023} & Divergent thinking\newline $O$: none identifiable & novelty or divergent-thinking rubric & not controlled & $\Gamma$: implicit frame (ideation)\newline $\mu$: not separated\\
34.~Verbal Creativity \citep{dinu_testing_2025} & Linguistic creativity\newline $O$: none identifiable & novelty or divergent-thinking rubric (originality, flexibility, elaboration) & not controlled & $\Gamma$: implicit frame (ideation)\newline $\mu$: not separated\\
35.~Automated Creativity \citep{li_automated_2025} & Creative writing\newline $O$: none identifiable (reference texts anchor quality, not claim truth) & quality or preference rubric (reference-text quality anchor) & not controlled & $\Gamma$: implicit frame (task)\newline $\mu$: not separated\\
36.~Hallucinating LLM Could Be Creative \citep{sun_hallucinating_2024} & Reasoning-path evaluation\newline $O$: mixed or task-dependent (task correctness; consistency/clustering are diagnostics) & mixed (correctness, consistency, diversity) & not controlled & $\Gamma$: partial (mixed frame)\newline $\mu$: not separated\\
\midrule
\multicolumn{5}{l}{\textbf{Nominal profile: constrained solution search}}\\
37.~MacGyver \citep{tian_macgyver_2025} & Problem solving\newline $O$: human adjudication (feasibility under stated constraints) & feasibility or correctness criterion (feasibility and efficiency) & not controlled & $\Gamma$: not separately represented\newline $\mu$: not scored\\
38.~EscapeBench \citep{qian_escapebench_2025} & Agent puzzles\newline $O$: environment or tool state (interactive game state) & feasibility or correctness criterion & not controlled or not applicable & $\Gamma$: not separately represented\newline $\mu$: not scored\\
39.~CreativEval \citep{delorenzo_creativeval_2024} & Code generation\newline $O$: environment or tool state (Verilog testbench outcomes) & novelty or divergent-thinking rubric (fluency, flexibility, originality, elaboration) & not controlled or not applicable & $\Gamma$: not separately represented\newline $\mu$: not scored\\
40.~NeoCoder \citep{lu_benchmarking_2025} & Code generation\newline $O$: environment or tool state (benchmark test execution) & mixed (correctness, constraint adherence, divergence) & not controlled or not applicable & $\Gamma$: not separately represented\newline $\mu$: not scored\\
41.~CreativeMath \citep{ye_assessing_2025} & Mathematical solution generation\newline $O$: mixed or task-dependent (reference problem/solutions and unanimous multi-LLM correctness judgment) & mixed (correctness and solution novelty) & not controlled & $\Gamma$: not separately represented\newline $\mu$: not scored\\
\bottomrule
\end{tabularx}
\end{table*}

\clearpage
\appendixsection{AA-Omniscience Leaderboard Snapshot}%
\phantomsection
\makeatletter\def\@currentlabel{C}\makeatother\label{app:omniscience}

Section~\ref{subsec:relative} cites AA-Omniscience as evidence that scale and
recency are not sufficient proxies for factual reliability.
This appendix gives the measurements behind that statement and the limits on
how far they can be read.

AA-Omniscience's full leaderboard set contains 6,000 short-answer questions
across 42 topics and six domains.
Models are instructed to answer when confident and otherwise abstain. An
automated grader assigns \textsc{correct}, \textsc{incorrect},
\textsc{partial answer}, or \textsc{not attempted}. The Omniscience Index
rewards correct answers and penalizes incorrect ones across the full set. The
benchmark-specific hallucination rate measures confidently wrong answers among
all non-correct answers. More precisely, let $n_{c}$, $n_{w}$, $n_{u}$, and
$n_{a}$ denote the counts in the four categories and let
$N=n_{c}+n_{w}+n_{u}+n_{a}$. The Omniscience Index is
$100(n_{c}-n_{w})/N$, and the AA-Omniscience hallucination rate is
$n_{w}/(n_{w}+n_{u}+n_{a})$ \citep{jackson_aaomniscience_2025}.
The complete 6,000-question leaderboard set is private; the released dataset
contains 600 questions and is intended for aggregate inspection rather than
fine-grained topic estimates
\citep{artificialanalysis_aaomniscience_public_2025}.

Figure~\ref{fig:aa-omniscience-snapshot} reports the August 26, 2026 snapshot.
The release-date panel plots the Omniscience Index rather than the
hallucination rate.

\begin{figure}[!ht]
\renewcommand*{\theHfigure}{C.\arabic{figure}}
\centering
\includegraphics[width=0.98\linewidth]{figures/AA-Omniscience Hallucination Rate vs- Total Parameters (26 Aug '26) (1).png}
\par\smallskip
\includegraphics[width=0.98\linewidth]{figures/AA-Omniscience Index - Score vs- Release Date (26 Aug '26).png}
\caption{\textbf{AA-Omniscience leaderboard snapshot, August 26, 2026.}
Top: AA-Omniscience hallucination rate versus disclosed total parameters for
selected open-weight model configurations (lower is better; logarithmic
$x$-axis). Bottom: Omniscience Index versus release date for selected model
configurations (higher is better). The dispersion shows that greater scale or
later release does not by itself guarantee better performance on the
corresponding metric. This is a cross-sectional snapshot, not a causal scaling
estimate or a longitudinal trend. Source and graphics: Artificial Analysis
\citep{artificialanalysis_omniscience_leaderboard_2026}.}
\label{fig:aa-omniscience-snapshot}
\end{figure}

This evidence is not a controlled scaling law or a longitudinal model-family
study.
The plotted configurations come from heterogeneous laboratories and differ in
architecture, training, post-training, inference settings, and parameter
disclosure; the parameter panel necessarily excludes models without a disclosed
total count.
The date panel does not control those choices, and the leaderboard's grader and
methodology changed between the 2025 launch analysis and this 2026 snapshot.
The figures therefore do not establish that hallucination is invariant over
time or inherent to every model-generation regime.
They complement the theoretical results discussed in
Section~\ref{subsec:relative}: those results motivate non-zero risk under
stated assumptions, while this snapshot shows that scale and recency are not
sufficient empirical proxies for factual reliability.

\clearpage
\bibliography{references}

\end{document}
