\documentclass[10pt]{article}

\usepackage[T1]{fontenc}
\usepackage[american]{babel}
\usepackage[a4paper,textwidth=32pc,top=12mm,bottom=14mm]{geometry}
\usepackage{natbib}
\bibliographystyle{compling}
\usepackage{mathtools}
\usepackage{amssymb}
\usepackage{graphicx}
\usepackage{xcolor}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,calc}
\usepackage[hidelinks]{hyperref}

\makeatletter
\setlength{\@fptop}{0pt}
\makeatother

\hypersetup{
  pdftitle={Ten Running-Example Figure Proposals},
  pdfsubject={Truth-contract examples for contract-relative hallucination evaluation}
}

\newcommand{\TC}{\textsc{TC}}
\newcommand{\LD}{\textsc{LD}}
\newcommand{\Hall}{\textsc{H}}
\newcommand{\SUP}{\textsc{SUP}}

\definecolor{clrH}{RGB}{155,18,18}
\definecolor{clrLD}{RGB}{22,98,48}
\definecolor{clrB}{RGB}{22,52,125}
\definecolor{clrP}{RGB}{92,61,145}
\definecolor{clrO}{RGB}{170,91,24}
\definecolor{clrG}{RGB}{65,65,65}
\definecolor{bgB}{RGB}{245,247,254}

\tikzset{
  input/.style={rectangle,rounded corners=4pt,draw=gray!55,fill=gray!8,
    font=\small\itshape,text width=5.0cm,align=center,inner sep=6pt},
  decision/.style={rectangle,rounded corners=3pt,draw=clrB!55,fill=bgB,
    font=\small,text width=5.2cm,align=center,inner sep=6pt},
  verdict/.style={rectangle,rounded corners=3pt,draw=clrG,fill=gray!8,
    font=\small\bfseries\sffamily,inner xsep=7pt,inner ysep=4pt},
  verdictH/.style={verdict,draw=clrH,text=clrH,fill=clrH!7},
  verdictLD/.style={verdict,draw=clrLD,text=clrLD,fill=clrLD!7},
  arrow/.style={-{Stealth[length=5pt,width=3.5pt]},thick,color=gray!70!black},
  edge/.style={font=\scriptsize\sffamily,fill=white,inner sep=1.5pt}
}

% Each proposal below is an author-constructed specification check adapted
% from the task design of a mapped resource.  None is represented as a sampled
% model output or as an original item from the cited benchmark.

\newcommand{\RenderProposal}{%
\begin{figure}[!t]
\centering
\resizebox{0.86\linewidth}{!}{%
\begin{tikzpicture}[x=1cm,y=1cm]
  \tikzset{
    taskbox/.style={
      decision,text width=5.35cm,minimum height=2.15cm,
      font=\footnotesize,inner sep=5pt},
    contractbox/.style={
      decision,text width=5.35cm,minimum height=1.55cm,
      font=\scriptsize,inner sep=4pt},
    contextbox/.style={
      rectangle,rounded corners=4pt,draw=clrO!60,fill=clrO!6,
      text width=10.8cm,align=left,font=\scriptsize,inner sep=5pt},
    sharedspan/.style={
      input,text width=8.7cm,minimum height=1.25cm,
      font=\footnotesize,inner sep=5pt},
    outcome/.style={
      font=\scriptsize,align=center,text width=3.75cm}
  }

  \draw[dashed,gray!50] (0,0.95) -- (0,-9.10);

  \node[taskbox] (strict) at (-3.0,0)
    {\textbf{\StrictTaskName}\\
     Literal prompt: ``\StrictPrompt''};
  \node[taskbox] (hyp) at (3.0,0)
    {\textbf{\PermissiveTaskName}\\
     Literal prompt: ``\PermissivePrompt''};

  \node[contextbox] (context) at (0,2.55)
    {\textbf{Task context $p$ --- task oracle $O$}\\[-1pt]
     \ContextText};

  \coordinate (branch) at ($(context.south)+(0,-0.30)$);
  \coordinate (strictAbove) at ($(strict.north)+(0,0.25)$);
  \coordinate (hypAbove) at ($(hyp.north)+(0,0.25)$);

  \draw[semithick,draw=black!55]
    (context.south) -- (branch);

  \draw[arrow]
    (branch) -| (strictAbove)
    -- (strict.north);

  \draw[arrow]
    (branch) -| (hypAbove)
    -- (hyp.north);

  \node[contractbox] (tc1) at (-3.0,-2.85)
    {$\TC(p_1)=(O,\Gamma_1,\mu)$\\
     $O$: \OracleShort\\
     $\Gamma_1=\varnothing$: \StrictScope\\
     $\mu$: uncertainty marked explicitly};
  \node[contractbox] (tc2) at (3.0,-2.85)
    {$\TC(p_2)=(O,\Gamma_2,\mu)$\\
     $O$: \OracleShort\\
     $\Gamma_2$: \PermissiveScope\\
     $\mu$: uncertainty marked explicitly};

  \node[
    rectangle,rounded corners=3pt,draw=clrG!45,fill=gray!4,
    font=\scriptsize,align=center,text width=5.0cm,inner sep=3pt
  ] (sharedstyle) at (0,-4.18)
    {Shared requested style-marking level: minimally marked
     (outside the truth contract)};

  \node[sharedspan] (span) at (0,-5.55)
    {\textbf{\upshape Identical response span:}
     ``\ResponseSpan''\\
     \textbf{\upshape Identical claim content:} \ClaimContent;
     \textbf{\upshape observed status marking:} uncertainty marked by
     ``\Marker''};

  \node[verdictH] (hall) at (-3.0,-7.15)
    {HALLUCINATION (H)};
  \node[verdictLD] (licensed) at (3.0,-7.15)
    {LICENSED DIVERGENCE (LD)};
  \node[outcome] at (-3.0,-7.78)
    {\textsc{unknown}; outside $\Gamma_1$\\
     reason code: \textsc{out-of-scope}};
  \node[outcome,text=clrLD] at (3.0,-7.78)
    {\textsc{unknown}; inside $\Gamma_2$\\
     and marked as required};

  \draw[arrow] (strict.south) -- (tc1.north);
  \draw[arrow] (hyp.south) -- (tc2.north);
  \draw[arrow] (tc1.south) -- (span.north -| tc1.south);
  \draw[arrow] (tc2.south) -- (span.north -| tc2.south);
  \draw[arrow] (span.south -| hall.north) -- (hall.north);
  \draw[arrow] (span.south -| licensed.north) -- (licensed.north);

  \node[
    rectangle,rounded corners=3pt,draw=clrG!45,fill=gray!4,
    font=\scriptsize\sffamily\bfseries,align=center,
    text width=7.7cm,inner sep=4pt
  ] at (0,-8.75)
    {Only the permission scope changes the claim label.};
\end{tikzpicture}%
}
\caption{\textbf{Proposal \ProposalNumber: \ProposalTitle.}
The response span, claim content, evidence state, observed status marking,
task oracle, required status marking, and requested style-marking level are
held fixed; only the permission scope changes.  The strict task therefore
assigns the hallucination label (\Hall) with reason code
\textsc{out-of-scope}, whereas the bounded task assigns licensed divergence
(\LD).  The latter records contract compliance, not truth, usefulness,
quality, creativity, or safety.  \SourceNote}
\label{fig:\ProposalLabel}
\end{figure}
}

\begin{document}

% Proposal 1 ---------------------------------------------------------------
\begingroup
\def\ProposalNumber{1}
\def\ProposalTitle{A Live Football Match}
\def\ProposalLabel{football}
\def\StrictTaskName{Factual match-update task}
\def\PermissiveTaskName{Match update with one prediction}
\def\StrictPrompt{Report the match using only the attached live feed. Mark any uncertainty explicitly.}
\def\PermissivePrompt{Report the match and add one plausible prediction for the remaining time. Mark any uncertainty explicitly.}
\def\ContextText{Time-stamped live feed at 70:00: ``Lions 1--1 Hawks.'' No later match events are available.}
\def\OracleShort{time-stamped live match feed}
\def\StrictScope{no predictions beyond the feed}
\def\PermissiveScope{one plausible match prediction}
\def\ResponseSpan{The Lions may score again before full time.}
\def\ClaimContent{the Lions score at least one more goal before full time}
\def\Marker{may}
\def\SourceNote{This author-constructed contrast is adapted from the time-indexed QA design of RealTime QA \citep{kasai_realtime_2024}; it is not a benchmark item or sampled model output.}
\RenderProposal
\endgroup
\clearpage

% Proposal 2 ---------------------------------------------------------------
\begingroup
\def\ProposalNumber{2}
\def\ProposalTitle{A Diverted Flight}
\def\ProposalLabel{flight}
\def\StrictTaskName{Factual flight-status task}
\def\PermissiveTaskName{Flight status with one diagnosis}
\def\StrictPrompt{Report the flight status using only the attached database record. Mark any uncertainty explicitly.}
\def\PermissivePrompt{Report the flight status and add one plausible cause of the diversion. Mark any uncertainty explicitly.}
\def\ContextText{Airline database record: ``Flight AB123 was diverted to Lake City.'' The record gives no cause for the diversion.}
\def\OracleShort{attached airline database record}
\def\StrictScope{no causes absent from the record}
\def\PermissiveScope{one plausible cause of diversion}
\def\ResponseSpan{Bad weather may have caused the diversion.}
\def\ClaimContent{bad weather caused the diversion}
\def\Marker{may}
\def\SourceNote{This author-constructed contrast is adapted from the external-tool QA design of ToolQA \citep{zhuang_toolqa_2023}; it is not a benchmark item or sampled model output.}
\RenderProposal
\endgroup
\clearpage

% Proposal 3 ---------------------------------------------------------------
\begingroup
\def\ProposalNumber{3}
\def\ProposalTitle{A Car Voice Assistant}
\def\ProposalLabel{car-voice}
\def\StrictTaskName{Manual-only support task}
\def\PermissiveTaskName{Support task with one hypothesis}
\def\StrictPrompt{Explain the voice-assistant problem using only the attached manual excerpt. Mark any uncertainty explicitly.}
\def\PermissivePrompt{Explain the problem and add one plausible cause not covered by the excerpt. Mark any uncertainty explicitly.}
\def\ContextText{Owner's-manual excerpt: ``Press and release the Voice Recognition button to activate the system.'' It lists no cause for failed speech detection.}
\def\OracleShort{attached owner's-manual excerpt}
\def\StrictScope{no undocumented failure causes}
\def\PermissiveScope{one plausible troubleshooting cause}
\def\ResponseSpan{The microphone opening may be blocked by dust.}
\def\ClaimContent{dust blocking the microphone opening causes the failed speech detection}
\def\Marker{may}
\def\SourceNote{This author-constructed contrast is adapted from DelucionQA's car-manual QA setting \citep{sadat_delucionqa_2023}; it is not a dataset item or sampled model output.}
\RenderProposal
\endgroup
\clearpage

% Proposal 4 ---------------------------------------------------------------
\begingroup
\def\ProposalNumber{4}
\def\ProposalTitle{A Closed Public Park}
\def\ProposalLabel{park}
\def\StrictTaskName{Strict news-summary task}
\def\PermissiveTaskName{News summary with one hypothesis}
\def\StrictPrompt{Summarize the notice using only information in the attached article. Mark any uncertainty explicitly.}
\def\PermissivePrompt{Summarize the notice and add one plausible explanation for the closure. Mark any uncertainty explicitly.}
\def\ContextText{Local-news excerpt: ``Riverside Park will remain closed on Monday.'' The article gives no reason for the closure.}
\def\OracleShort{attached local-news article}
\def\StrictScope{no explanations absent from the article}
\def\PermissiveScope{one plausible explanation}
\def\ResponseSpan{Strong winds may have caused the closure.}
\def\ClaimContent{strong winds caused Riverside Park to close}
\def\Marker{may}
\def\SourceNote{This author-constructed contrast is adapted from the source-grounded summarization setting studied with XSum \citep{narayan_xsum_2018,maynez_faithfulness_2020}; it is not a corpus item or sampled model output.}
\RenderProposal
\endgroup
\clearpage

% Proposal 5 ---------------------------------------------------------------
\begingroup
\def\ProposalNumber{5}
\def\ProposalTitle{A Town Clock Tower}
\def\ProposalLabel{clock-tower}
\def\StrictTaskName{Strict encyclopedia task}
\def\PermissiveTaskName{Encyclopedia task with one hypothesis}
\def\StrictPrompt{Explain why the tower was built using only the attached encyclopedia excerpt. Mark any uncertainty explicitly.}
\def\PermissivePrompt{Explain what is known and add one plausible motive for building the tower. Mark any uncertainty explicitly.}
\def\ContextText{Encyclopedia excerpt: ``The Harbor Clock stands in Bellport's central square. It opened in 1912.'' The excerpt gives no motive for its construction.}
\def\OracleShort{attached encyclopedia excerpt}
\def\StrictScope{no motives absent from the excerpt}
\def\PermissiveScope{one plausible historical motive}
\def\ResponseSpan{Growing rail traffic may have motivated its construction.}
\def\ClaimContent{growing rail traffic motivated construction of the Harbor Clock}
\def\Marker{may}
\def\SourceNote{This author-constructed contrast is adapted from FEVER's evidence-based claim-verification design \citep{thorne_fever_2018}; it is not a FEVER claim or sampled model output.}
\RenderProposal
\endgroup
\clearpage

% Proposal 6 ---------------------------------------------------------------
\begingroup
\def\ProposalNumber{6}
\def\ProposalTitle{An Unfamiliar Basketball Move}
\def\ProposalLabel{basketball}
\def\StrictTaskName{Strict sports-glossary task}
\def\PermissiveTaskName{Sports invention task}
\def\StrictPrompt{Define the term using only the attached basketball glossary. Mark any uncertainty explicitly.}
\def\PermissivePrompt{Use the glossary and propose one possible definition for the new move. Mark any uncertainty explicitly.}
\def\ContextText{Basketball glossary: ``A crossover changes dribbling direction; a jump stop lands on both feet.'' It contains no entry for ``turbo-jump dribble.''}
\def\OracleShort{attached basketball glossary}
\def\StrictScope{no definitions for unlisted moves}
\def\PermissiveScope{one proposed definition for the new move}
\def\ResponseSpan{A turbo-jump dribble may combine a jump with a rapid change of direction.}
\def\ClaimContent{a turbo-jump dribble combines a jump with a rapid change of direction}
\def\Marker{may}
\def\SourceNote{This author-constructed contrast is adapted from HypoTermQA's hypothetical-term evaluation design \citep{uluoglakci_hypotermqa_2024}; it is not a dataset item or sampled model output.}
\RenderProposal
\endgroup
\clearpage

% Proposal 7 ---------------------------------------------------------------
\begingroup
\def\ProposalNumber{7}
\def\ProposalTitle{Reaching Cookies in a Deep Jar}
\def\ProposalLabel{cookie-jar}
\def\StrictTaskName{Verified-affordance task}
\def\PermissiveTaskName{Creative problem-solving task}
\def\StrictPrompt{Describe only uses of the available objects that have been verified. Mark any uncertainty explicitly.}
\def\PermissivePrompt{Propose one plausible way to reach the cookies with the available objects. Mark any uncertainty explicitly.}
\def\ContextText{Problem card: ``The jar is narrow and deep. Available objects: chopsticks, tape, and rubber bands.'' No solution has been tested.}
\def\OracleShort{problem card and tested environment state}
\def\StrictScope{no untested object uses}
\def\PermissiveScope{one candidate use of the objects}
\def\ResponseSpan{Taping the chopsticks together may create long tongs.}
\def\ClaimContent{taped chopsticks function as long tongs for reaching the cookies}
\def\Marker{may}
\def\SourceNote{This author-constructed contrast is adapted from MacGyver's constrained creative-problem-solving design \citep{tian_macgyver_2025}; it is not a benchmark item or sampled model output.}
\RenderProposal
\endgroup
\clearpage

% Proposal 8 ---------------------------------------------------------------
\begingroup
\def\ProposalNumber{8}
\def\ProposalTitle{A Rusty Key in an Escape Room}
\def\ProposalLabel{escape-room}
\def\StrictTaskName{Observed-state reporting task}
\def\PermissiveTaskName{Exploratory planning task}
\def\StrictPrompt{Report only what the room log establishes about the key and box. Mark any uncertainty explicitly.}
\def\PermissivePrompt{Report the state and propose one plausible next action. Mark any uncertainty explicitly.}
\def\ContextText{Room log: ``A rusty key, a locked box, and a small bottle of oil are present.'' No action has yet been attempted.}
\def\OracleShort{observed room and action log}
\def\StrictScope{no untested action outcomes}
\def\PermissiveScope{one candidate next action and outcome}
\def\ResponseSpan{Applying the oil may make the rusty key usable.}
\def\ClaimContent{applying the oil makes the rusty key usable}
\def\Marker{may}
\def\SourceNote{This author-constructed contrast is adapted from EscapeBench's interactive room-escape design \citep{qian_escapebench_2025}; it is not a benchmark state or sampled model output.}
\RenderProposal
\endgroup
\clearpage

% Proposal 9 ---------------------------------------------------------------
\begingroup
\def\ProposalNumber{9}
\def\ProposalTitle{A Higher Household Energy Bill}
\def\ProposalLabel{energy-bill}
\def\StrictTaskName{Strict report-summary task}
\def\PermissiveTaskName{Report summary with one diagnosis}
\def\StrictPrompt{Summarize the household report using only the attached figures. Mark any uncertainty explicitly.}
\def\PermissivePrompt{Summarize the report and add one plausible explanation for the increase. Mark any uncertainty explicitly.}
\def\ContextText{Energy report: ``Electricity use was 15\% higher this month than last month.'' The report does not record weather or appliance use.}
\def\OracleShort{attached household energy report}
\def\StrictScope{no causes absent from the report}
\def\PermissiveScope{one plausible cause of the increase}
\def\ResponseSpan{Colder weather may have increased heating use.}
\def\ClaimContent{colder weather increased household heating use}
\def\Marker{may}
\def\SourceNote{This author-constructed contrast is adapted from HalluRAG's closed-domain grounding design \citep{ridder_hallurag_2025}; it is not a dataset item or sampled model output.}
\RenderProposal
\endgroup
\clearpage

% Proposal 10 --------------------------------------------------------------
\begingroup
\def\ProposalNumber{10}
\def\ProposalTitle{Adapting a Cake Recipe}
\def\ProposalLabel{cake}
\def\StrictTaskName{Strict recipe-restatement task}
\def\PermissiveTaskName{Recipe task with one experiment}
\def\StrictPrompt{Restate the baking instructions using only the attached recipe card. Mark any uncertainty explicitly.}
\def\PermissivePrompt{Restate the instructions and suggest one clearly marked baking experiment. Mark any uncertainty explicitly.}
\def\ContextText{Recipe card: ``Mix the batter and bake for 25 minutes at 180\,$^\circ$C.'' It gives no instruction about chilling the batter.}
\def\OracleShort{attached recipe card}
\def\StrictScope{no techniques absent from the card}
\def\PermissiveScope{one proposed baking experiment}
\def\ResponseSpan{Chilling the batter may produce a more even rise.}
\def\ClaimContent{chilling the batter produces a more even rise}
\def\Marker{may}
\def\SourceNote{This author-constructed contrast is adapted from WritingBench's material-conditioned generative-writing design \citep{wu_writingbench_2025}; it is not a benchmark prompt or sampled model output.}
\RenderProposal
\endgroup
\clearpage

\bibliography{references}

\end{document}
